# 세션 카탈로그

실제로 공개된 장애·사고와 널리 알려진 함정 **169건**의 재현 설계 목록입니다.
세션을 시작할 때 해당 항목을 열고, 끝나면 하단 체크리스트에 표시합니다.

---

## 0. 이 문서 사용법

### 0-1. 정체성
교과서 개념을 실측하는 원리 실험(구 트랙 C)은 **폐기했다.** 격리수준·MVCC·인덱스 같은 기본기는 별도 시리즈가 아니라 **각 사건 편의 "내부 원리" 절에서 필요한 만큼만** 설명한다(갭 락 데드락 편에서 격리수준을, UUID 페이지 스플릿 편에서 B-Tree를).

### 0-2. 산출물 규격
- **저장소(예정)**: `~/Desktop/db-incident-lab` — 공개 여부는 시작 시 결정
- **세션당 산출물**: `NN-슬러그/` 에 ①재현 환경(compose.yml/스크립트) ②`reproduce.md`(실제 명령·출력 원문) ③전후 실측 ④`README.md` → 회고 글 1편
- **편당 고정 구조**: 유명한 이유(출처 명시) → 재현(내 수치) → 내부 원리 → 해소 → 재계측 → **예상과 달랐던 점 1개(필수)**
- 문체: 합니다체, 실측 블록, 이모지 0, 문중 대시 0. 서술 템플릿 참고는 [LINE 장애 대응 프로세스 글](https://engineering.linecorp.com/ko/blog/line-platform-server-outage-process-and-dev-culture)

### 0-3. 근거 등급 (편마다 반드시 명시 — 과장 방지)
| 등급 | 뜻 | 글에서 쓰는 표현 |
|---|---|---|
| **E1** | 기업 포스트모템·규제기관 문서 원문 존재 | "○○사가 공개한 장애" |
| **E2** | 특정 사건은 아니나 벤더 공식 문서·CVE·업계 통계가 경고 | "실무에서 반복되는 함정이고 공식 문서가 별도 경고를 낼 정도" |
| **E1·축소** | 사건은 실재하나 물리·관리형 인프라라 그대로는 불가 | "사건 자체는 재현할 수 없어 같은 메커니즘을 축소 재현했다" |
| **E4** | 출처 미확보 | **블로그화 금지** (→ §13에서 전부 정리·폐기 완료) |

### 0-4. 재현 도구 스택 (검증됨)
Toxiproxy(지연·끊김 주입) · Pumba(컨테이너 kill/pause/netem) · dnsmasq/CoreDNS(DNS 전환) · tc netem(리전 RTT) · PgBouncer/ProxySQL(RDS Proxy 대역) · Orchestrator/Patroni(자동 페일오버) · pg_prewarm·buffer pool dump(캐시 워밍) · k6/sysbench(부하) · Verdaccio(사설 레지스트리)

### 0-5. ★ 우선순위 기준
- **★★★** 우선 재현 대상. 사건의 유명도·재현 증거의 명확성·기술 밀도가 모두 높음
- **★★** 재현 가치는 충분하나 우선순위가 한 단계 아래
- **★** 난이도가 높거나 다른 세션과 소재가 겹침

---

## 1. 전체 지도

| 트랙 | 개수 | 다루는 것 | 분류 | 대표 3개 |
|---|---|---|---|---|
| **[A](#2-트랙-a--dba-21)** | 21 | DB 내부·운영·복제 | DBA·DBRE | A02 MDL 폭풍 · A17 UUID 스플릿 · A19 SLRU 절벽 |
| **[B](#3-트랙-b--백엔드-50)** | 50 | 앱 코드·설정이 원인 | 백엔드 | B14 정규식 백트래킹 · B30 무한큐 OOM · B43 expand-contract |
| **[I](#4-트랙-i--인프라sre-11)** | 11 | 컨테이너·커널·네트워크·배포 | 인프라·SRE | I02 CPU 스로틀링 · I07 롤링 유실 · I11 Toyota |
| **[R](#5-트랙-r--클라우드-관리형-db-18)** | 18 | RDS·Aurora 운영 | 클라우드 DBA | R01 승격 유실 · R13 슬롯 카운터 · R02 DNS 캐시 |
| **[S](#6-트랙-s--검색엔진-12)** | 12 | ES·Lucene 운영 | 검색 | S05 Nori 함정 · S02 딥 페이지네이션 · S01 매핑 폭발 |
| **[V](#7-트랙-v--벡터dbrag-8)** | 8 | 벡터 검색 품질 | AI·벡터 | V01 필터 recall · V02 HNSW 스윕 · V03 연산자 불일치 |
| **[L](#8-트랙-l--llmrag-운영-10)** | 10 | LLM 서빙·에이전트·비용 | AI(FDE·PDE) | L01 무한 루프 · L02 인젝션 · L05 청킹 |
| **[M](#9-트랙-m--모바일앱웹-연동-14)** | 14 | 백엔드가 모바일을 상대할 때 | 백엔드 확장 | M01 푸시 herd · M02 멱등성 · M03 토큰 레이스 |
| **[F](#10-트랙-f--금융증권-it-21)** | 21 | 증권·금융 도메인 | 금융IT | F01 한맥 · F07 IPO 라이브락 · F13 매칭엔진 |
| **[X](#11-트랙-x--축소-재현-설계-4)** | 4 | 물리 사건의 축소 재현 | 공통 | X4 카카오 DC · X3 부분 HA |
| **합계** | **169** | | | |

---

## 2. 트랙 A — DBA (21)

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| A01 ★★★ | **정수 PK 고갈** | **[E1]** Basecamp 2018 int 최대치로 5h 쓰기 중단, GitLab 전사 bigint 전환 (crunchydata.com) | 시퀀스를 21억 직전으로 → INSERT 에러 → 무중단 bigint 마이그레이션(신규 컬럼+백필+스왑). MySQL·PG 비교 | 쉬움 |
| A02 ★★★ | **온라인 DDL MDL 폭풍** | **[E2]** 롱 쿼리 + ALTER의 메타데이터 락 대기가 뒤의 SELECT까지 전부 블록(MySQL 대표 함정) | 세션1 장기 SELECT → 세션2 ALTER → k6 트래픽 전면 블록 → gh-ost·LOCK=NONE·lock_wait_timeout 비교 | 쉬움 |
| A03 ★ | **utf8 vs utf8mb4** | **[E2]** MySQL utf8=3바이트 가짜 UTF-8, 이모지 절단 만년 함정 | 이모지 INSERT 에러 → utf8mb4 전환 + 인덱스 767바이트 제한 | 쉬움 |
| A04 ★★ | **MSSQL 락 에스컬레이션** | **[E2]** 약 5,000 로우 락 초과 시 테이블 락 승격(금융권 상용 DBMS 운영) | 대량 UPDATE → sys.dm_tran_locks 관찰 → LOCK_ESCALATION·배치 분할 | 쉬움 |
| A05 ★★ | **커넥션 폭풍 + PgBouncer** | **[E2]** PG 프로세스 모델에서 재배포 직후 대량 재연결로 DB는 정상인데 서비스 마비 | max_connections 축소 + k6 → FATAL too many connections → PgBouncer 전후 | 쉬움 |
| A06 ★★★ | **갭 락 데드락** | **[E2]** REPEATABLE READ 갭 락·insert intention 조합, 실무 데드락 최다 빈출 (Tecoble·우형) | 두 세션 SELECT FOR UPDATE 범위 겹침+INSERT → **SHOW ENGINE INNODB STATUS 그래프 해석이 백미** → 해소 3안 | 중간 |
| A07 ★★ | **롱 트랜잭션 2차 피해** | **[E2]** idle in transaction 하나가 undo 폭증(MySQL)·VACUUM 무력화(PG) | 트랜잭션 열어두고 UPDATE 폭탄 → history list length·n_dead_tup → 종료 후 회복 | 쉬움 |
| A08 ★ | **buffer pool 사이징** | **[E2]** DBA 파라미터 튜닝의 대표 주제 | innodb_buffer_pool_size 극단 변경 + 동일 부하 → hit ratio·p95 전후 | 쉬움 |
| A09 ★★★ | **통계 오염 플랜 붕괴** | **[E1]** GoCardless + **Clerk 2026-02 실장애**(ANALYZE 샘플이 NULL만 잡아 오판) | 분포 왜곡 → ANALYZE 전후 플랜 플립 → statistics_target·확장 통계 방어 | 중간 |
| A10 ★★ | **복제 지연 관측** | **[E2]** R/W 분리의 통과의례, Seconds_Behind_Master의 거짓말 | 대량 UPDATE로 지연 유발 → SBM 진동 vs GTID 실지연 비교 → 병렬 복제 파라미터 | 중간 |
| A11 ★★★ | **세미싱크 페일오버 유실** | **[E2]** async 폴백 순간 마스터 사망 → 커밋 응답 받은 트랜잭션 유실 (PlanetScale) | 2노드 semi-sync → 폴백 유도 → kill → 승격 레플리카에 없음(GTID 증명) | 중간 |
| A12 ★★★ | **GitHub 2018 스플릿 브레인** | **[E1·축소]** 43초 단절로 양쪽 프라이머리 분기, 24h 장애 (github.blog, 원문 확인) | Docker 네트워크 2개=리전 + Orchestrator → disconnect → 분기 → GTID로 페일백 불가 증명 | 어려움 |
| A13 ★★ | **GitLab 2017 복제 재구축 실패** | **[E1·축소]** 잘못된 노드 rm -rf + 백업 5중 실패 (about.gitlab.com, 원문 확인) | WAL 세그먼트 유실 → 복제 단절 → pg_basebackup 재구축 재연. **"복제 재구축" 초점**(백업/PITR과 분리) | 중간 |
| A14 ★★ | **XID Wraparound** | **[E2]** PG 최악 장애, 프로덕션 셧다운 사례 (Percona) | autovacuum_freeze_max_age 극단 축소 → age 증가·경고·aggressive vacuum 관측(방어선까지만) | 중간 |
| A15 ★ | **fsyncgate** | **[E1·축소]** 리눅스 fsync 실패 후 조용한 유실, 2018 PANIC 전환 (danluu.com) | privileged + device-mapper flakey 위에 PG 데이터 디렉토리 → I/O 오류 주입 → `data_sync_retry` on/off. 대안: LD_PRELOAD로 EIO 주입 | 어려움 |
| A16 ★★★ | **OOM Killer가 DB를 쏜다** | **[E2]** DB는 oom_score 1순위 타깃, Percona·Crunchy "The Linux Assassin" | docker --memory 제한 + 대량 정렬 → OOM kill → dmesg·크래시 리커버리 → oom_score_adj·overcommit | 쉬움 |
| A17 ★★★ | **UUIDv4 PK 페이지 스플릿** | **[E2]** Percona·PlanetScale 반복 경고. 랜덤 PK가 페이지 충전율 붕괴·테이블 2배·insert 절벽 | AUTO_INCREMENT vs UUIDv4 vs UUIDv7 수백만 행 insert → 시간·크기·buffer_pool_reads 비교 | 쉬움 |
| A18 ★★ | **Uber Postgres 쓰기 증폭** | **[E1]** DB 커뮤니티 사상 최다 논쟁 글(2016). 세컨더리 인덱스 전부 갱신·HOT 실패 | 인덱스 10개 테이블에서 무관 컬럼 UPDATE → PG vs MySQL WAL량·인덱스 갱신 계측. **논쟁 글을 직접 검증하는 구도** | 중간 |
| A19 ★★★ | **GitLab 서브트랜잭션 SLRU 절벽** | **[E1]** 몇 주마다 랜덤 발생한 "네시" 장애. SAVEPOINT 64개 초과+롱 트랜잭션 → replica TPS 360k→50k (공개 재현 스크립트 존재) | primary+replica, pgbench에 SAVEPOINT 중첩 → TPS 절벽. **ORM이 SAVEPOINT를 몰래 만든다는 각도가 백미** | 중간 |
| A20 ★★ | **데브시스터즈 CockroachDB 쿼럼 손실** | **[E1]** 쿠키런 킹덤 36시간 복구, 국내 전설 회고 (tech.devsisters.com) | CRDB 3노드 → 2노드 kill → Raft 쿼럼 손실로 SQL 전면 중단(비트 복구는 원문 소개로) | 중간 |
| A21 ★★ | **Figma식 수직 분할·논리 복제 이전** | **[E1]** 단일 RDS 포화 → 테이블 그룹 분리, 논리 복제 무중단 이전 (figma.com) | PG 2대 + logical replication으로 특정 테이블만 무중단 이전 → 커넥션 스위칭 | 중간 |


---

## 3. 트랙 B — 백엔드 (50)

### 3-1. 부하·트래픽

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| B01 ★★★ | **HikariCP 풀 데드락** | **[E1]** 우형 메시지 플랫폼 실장애, pool size 공식의 출처 | 요청당 커넥션 2개 + pool 10 + k6 20 → 전면 멈춤 → 공식 재계산 전후 | 쉬움 |
| B02 ★★ | **캐시 스탬피드** | **[E2]** 인기 키 TTL 동시 만료 → DB 직행 폭주(실무 빈출) | 만료 시점 k6 500 → 쿼리 폭주 → 뮤텍스/확률적 조기 갱신/jittered TTL 3안 | 쉬움 |
| B03 ★★ | **read-your-writes** | **[E2]** "방금 쓴 게 안 보여요" CS의 원인 | 지연 상태에서 쓰기 직후 replica 읽기 → 세션 고정/임계 라우팅/primary 3안 | 쉬움(A10 선행) |
| B04 ★ | **깊은 페이지네이션** | **[E2]** OFFSET 딥 페이지 비용, 실행계획 설명에 최적 | OFFSET 100만 vs 커서 + 실행계획 비교 | 2~3시간 |
| B05 ★★★ | **리트라이 폭풍 / metastable** | **[E2]** HotOS 2021 논문으로 개념화, AWS 장애 복구 지연의 원인 | 짧은 타임아웃+무제한 재시도 → **부하를 내려도 회복 못 하는 그래프** → 백오프+지터+재시도 예산 | 중간 |
| B06 ★★ | **핫 로우 카운터 경합** | **[E2]** 단일 카운터 UPDATE 몰림 → 처리량 붕괴 | 1행 동시 UPDATE 락 대기 → 카운터 샤딩·Redis 집계 전후 | 쉬움 |
| B07 ★★ | **핫 파티션 + request coalescing** | **[E1]** Discord가 인기 채널 쏠림을 동일 키 합치기로 해결 (InfoQ) | 단일 키 부하 쏠림 → singleflight식 coalescing 전후 | 중간 |
| B12 ★★ | **Facebook memcache lease** | **[E2]** NSDI 2013 논문의 thundering herd 해법 | B02 환경에서 lease 토큰 직접 구현 → 스탬피드 해법 4안째로 비교 | 반나절(B02 선행) |
| B23 ★★ | **Shopify 테넌트 격리** | **[E1]** 한 상점 플래시 세일이 전체 용량 20% 잠식 → pod 격리 | 단일 DB vs 테넌트 분리 2DB에서 폭주 시 지연 격리 비교 | 중간 |

### 3-2. 동시성·트랜잭션

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| B24 ★★★ | **Redis 분산 락 만료 + 펜싱 토큰** | **[E2]** Kleppmann: "GC 정지가 락 리스보다 길면 클라이언트는 만료된 줄 모르고 쓴다". Redlock은 **펜싱 토큰이 없어 근본적으로 안전하지 않다** → antirez 반박 논쟁 (원문 확인) | TTL 2초 + 임계구역 3초 sleep(또는 SIGSTOP) → 두 워커 동시 진입 → **"마지막 토큰보다 낮으면 거부"** 펜싱 전후 | 쉬움~중간 |
| B25 ★★★ | **@Transactional self-invocation** | **[E2]** 같은 클래스의 `this.method()`는 AOP 프록시를 우회해 **트랜잭션이 시작조차 안 됨** → 예외에도 롤백 없이 부분 저장 | 내부 호출 vs 별도 빈 버전 롤백 비교. 전파 오설정·readOnly 함정까지 묶기 | 쉬움 |
| B26 ★★ | **트랜잭션 안에서 외부 API 호출** | **[E2]** DB 커넥션을 쥔 채 HTTP 대기 → 풀 고갈 + 외부 호출은 롤백 불가 | @Transactional 안에서 3초 지연 목 호출 → 풀 점유 그래프 → 밖으로 빼는 리팩터링 전후 | 쉬움 |

### 3-3. 메시징·스케줄링

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| B09 ★★ | **Kafka 슬로우 컨슈머·리밸런스 스톰** | **[E2]** 컨슈머를 늘리면 리밸런스로 lag이 더 커지는 역설 | 느린 컨슈머 + 컨슈머 추가 → 악화 재현 → cooperative rebalancing·백프레셔 | 중간 |
| B27 ★★★ | **exactly-once 신화와 멱등 컨슈머** | **[E2]** "네트워크 계층의 진짜 exactly-once는 불가능", Kafka 기본은 at-least-once. 트랜잭션 경계 밖 시스템은 보장 안 됨 | 오프셋 커밋 **직전** 컨슈머 kill → 재기동 시 이중 과금 → 유니크 키 upsert 멱등 컨슈머 전후 | 중간 |
| B28 ★★ | **poison message와 DLQ 무한 루프** | **[E2]** 잘못된 1건이 6시간·1만 회 재시도로 파티션 전체를 막은 결제사 사례. DLQ replay가 장애를 재생산하는 안티패턴 | 실패 메시지가 뒤를 막는 상황 → maxReceiveCount+DLQ 격리 → **replay 루프백까지** 시연 | 쉬움~중간 |
| B29 ★★★ | **스케줄러 멀티 인스턴스 중복 실행** | **[E2]** 파드 여럿이 같은 cron을 동시 실행해 이중 정산. Quartz 클러스터 모드에서도 트리거 이중 획득 사례 | `compose --scale app=3` → 카운터 3배 → **ShedLock 적용 후 1회 수렴**. 분산 락으로 단일 실행을 보장하는 표준 해법 | 쉬움 |

### 3-4. 리소스 누수·고갈

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| B08 ★★ | **GC 스톨 연쇄 타임아웃** | **[E2]** JVM STW → 상류 타임아웃·재시도 연쇄 | 작은 힙 + 할당 폭탄 + k6 → GC 로그와 상류 타임아웃·서킷브레이커 연결 | 중간 |
| B30 ★★★ | **스레드풀 무한대 큐 OOM** | **[E2]** `Executors.newFixedThreadPool()`이 **무제한 LinkedBlockingQueue**를 써서 유입>처리면 큐가 무한 증가. "무제한 큐는 프로덕션의 조용한 살인자" | `-Xmx128m` + 느린 태스크 폭주 → OOM → **힙 덤프로 LinkedBlockingQueue$Node 시각화** → 바운디드 큐+CallerRunsPolicy | 쉬움 |
| B31 ★★ | **ThreadLocal·ClassLoader 누수** | **[E2]** remove() 누락 시 워커 스레드가 클래스로더를 붙잡아 재배포 반복에 Metaspace OOM. Tomcat이 경고를 찍는 고전 | WAR 반복 핫디플로이 + MaxMetaspaceSize 축소 → OOM (경량화: 스레드풀에 ThreadLocal 심고 반복) | 중간 |
| B32 ★★ | **파일 디스크립터 누수** | **[E2]** close 누락으로 `Too many open files`. 원인은 HTTP·JDBC 커넥션 미반환·스트림 미종료·JNI 누수. **대표 단일 사건은 없음(§13 참조)** | ulimit -n 256 + close 안 하는 클라이언트 반복 → `/proc/[PID]/fd`·lsof로 증가 관측 → try-with-resources 비교 | 쉬움 |
| B51 ★★ | **ForkJoinPool commonPool 오염** | **[E2]** parallelStream이 **JVM 전역 commonPool**을 공유(Baeldung). 여기서 블로킹 IO를 돌리면 무관한 병렬 작업까지 굶음. **국내 실장애 회고 "parallelStream 남용으로 인한 장애 경험기"(2019) 존재** | commonPool에서 긴 블로킹 작업 점유 → 다른 parallel stream 대기 관측 → 전용 풀 지정 비교 | 쉬움 |

### 3-5. 타임아웃·네트워크 계층

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| B33 ★★★ | **LB idle timeout < 앱 keep-alive → 간헐 502** | **[E2]** 백엔드가 먼저 닫은 커넥션을 프록시가 재사용하려다 502. **"간헐적 502의 1순위 원인"** (iximiuz, gunicorn/ELB 사례, 원문 확인) | nginx + 백엔드 keep-alive를 프록시 idle보다 짧게 → 간헐 502 → 순서 뒤집으면 소멸 | 중간 |
| B34 ★★ | **클라이언트 타임아웃 < 서버 타임아웃** | **[E2]** 클라가 먼저 끊고 재시도하는데 서버는 원 요청을 계속 처리 → 인플라이트 배가. 원칙은 **계층 정렬(client > 각 홉 합)** | 느린 엔드포인트 + 짧은 클라 타임아웃 + 재시도 → 인플라이트 폭증 → **데드라인 전파**로 방어 | 쉬움 |
| B10 ★★ | **클럭 스큐(시간 역행)** | **[E1]** Cloudflare 2017 윤초로 음수 duration → panic, Go monotonic clock 도입 계기 | libfaketime으로 시간 역행 → wall clock duration 음수 버그 → monotonic clock 비교 | 중간 |
| B20 ★ | **500마일 이메일 (타임아웃=거리)** | **[E1]** 전설의 디버깅 스토리 (ibiblio.org) | tc netem 지연 1/5/50ms + connect timeout 3ms → "지연이 곧 거리" 시연 | 중간 |
| B11 ★★ | **우형 Redis New Connection** | **[E1]** 풀 LIFO·eviction 설정이 맞물려 신규 커넥션 지속 생성 (techblog.woowahan.com/23121) | Lettuce/Jedis 풀 설정 재현 → 커넥션 생성 지표 설정별 비교 | 중간 |

### 3-6. 데이터·스키마 함정

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| B15 ★★ | **ORDER BY RAND() 폭탄** | **[E2]** StackOverflow 단골, 100만 행 58분 vs 0.6초 | 100만 행 EXPLAIN·시간 비교 → 인덱스 기반 대안 | 쉬움 |
| B16 ★★★ | **Soft Delete unique 충돌** | **[E2]** "탈퇴한 이메일로 재가입 불가" + deleted_at 누락 시 삭제 데이터 누수 | 재가입 실패 재현 → PG partial index vs MySQL generated column 비교 | 쉬움 |
| B17 ★★★ | **Vancouver 지수 절삭 (FLOAT 돈 계산)** | **[E1]** 1982년 절삭 누적으로 지수가 22개월 만에 절반으로. 부동소수점 돈 계산의 역사적 교본 | float 절삭 누적 시뮬레이션 + FLOAT vs DECIMAL 합계 오차 | 쉬움 |
| B18 ★★ | **DST 존재하지 않는 시간** | **[E2]** Jira가 공식 KB를 낼 정도의 반복 함정(HOUR_OF_DAY 예외) | TZ America/New_York에서 DST 갭 시각 저장 → 예외 재현 → UTC 저장 원칙 | 쉬움 |
| B49 ★★ | **Protobuf 필드 번호 재사용** | **[E2]** 은퇴 번호를 재사용하면 구 바이너리가 새 의미로 **조용히 잘못 해석**("크래시 없이 틀린 결과") | v1 바이트를 번호 재사용 v2로 역직렬화 → 값 오염 → `reserved`·레지스트리 BACKWARD 체크 | 쉬움 |
| B13 ★ | **Notion식 샤딩·재샤딩** | **[E1]** Postgres 모놀리스 한계와 수평 샤딩 전환기 | PG 여러 대 + workspace_id 라우팅 → 더블 라이트→백필→검증→스위치오버 축소 재연 | 이틀+ |

### 3-7. 보안 (전부 격리된 로컬에서만, 외부 대상 금지)

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| B35 ★★★ | **Spring Actuator /heapdump 노출** | **[E1]** 노출 heapdump에서 자격증명 추출 → 폭스바겐 텔레매틱스 **GPS 9TB 유출**. Wiz 조사상 클라우드 환경 **11%가 공개 노출** | management 전부 노출 → `curl /actuator/heapdump` → 힙에서 시크릿 grep → 제한 후 재확인. **임팩트 대비 난이도 최저** | 쉬움 |
| B36 ★★★ | **JWT alg=none / 서명 미검증** | **[E2]** 초기 다수 라이브러리가 `alg: none`을 유효로 취급(CVE-2015-9235 계보). 대소문자 변형·RS256↔HS256 confusion까지 | 알고리즘 미고정 로직 → 헤더 none + role=admin 위조로 인증 우회 → 화이트리스트·키 고정 | 쉬움 |
| B37 ★★ | **압축 폭탄(gzip/zip bomb)** | **[E2]** 수 KB가 수 GB로 팽창. 42.zip·4.5PB 플랫 폭탄, OpenTelemetry Collector·Fluentd 실 CVE | 무한 해제 앱 + 메모리 제한 + 작은 폭탄 → OOM → LimitReader 상한 | 쉬움 |
| B38 ★★ | **Log4Shell (CVE-2021-44228)** | **[E1]** 로그의 `${jndi:ldap://...}` 치환으로 RCE, CVSS 10.0 (NVD 확인) | 취약 버전 + 로컬 LDAP(marshalsec) + 페이로드 서버. **부담되면 아웃바운드 콜백 관측까지만** | 중간 |
| B39 ★★ | **XXE·billion laughs** | **[E2]** 엔티티 재귀 확장으로 소량 입력이 GB급 팽창, XXE는 파일 유출·SSRF까지 (OWASP 확인) | 기본 파서에 billion laughs → 메모리 폭증 → DTD·외부 엔티티 비활성화 | 쉬움 |
| B40 ★★ | **Capital One SSRF → IMDSv1 탈취** | **[E1]** WAF의 SSRF로 EC2 메타데이터를 질의해 IAM 자격증명 획득, 1억 명 유출. **IMDSv2 도입 계기** | 사용자 URL을 서버가 fetch하는 SSRF + 목 메타데이터 서버 → 자격증명 유출 시연 | 중간 |
| B41 ★ | **Java 역직렬화 gadget chain** | **[E2]** commons-collections 체인으로 WebLogic·JBoss·Jenkins 동시 RCE. **classpath 내 기존 코드만으로** 연쇄 | 취약 버전 + ObjectInputStream → ysoserial 페이로드 → ObjectInputFilter 방어 | 중간 |
| B42 ★ | **xz-utils 백도어 (CVE-2024-3094)** | **[E1]** sshd 로그인 지연 0.3→0.8초를 이상히 여겨 발견. **git엔 없고 릴리스 tarball에만 있는** 빌드 스크립트가 liblzma 하이재킹 | **실 백도어 재현 금지.** "tarball에만 있는 악성 빌드 스크립트가 산출물을 바꾸는" 원리만 무해한 데모로 | 어려움 |

### 3-8. 배포·운영 사고

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| B43 ★★★ | **expand-contract 마이그레이션 실패** | **[E2]** 컬럼 추가·삭제 순서가 뒤집히면 배포 중 없는 컬럼 참조. 대형 테이블 `ADD COLUMN NOT NULL`이 ACCESS EXCLUSIVE 락으로 전 쿼리 차단 | 수백만 행 + 트래픽 중 ALTER → 락 대기 타임아웃 → **3단계(nullable→백필→제약)** 대비 | 쉬움~중간 |
| B44 ★★★ | **"설정 배포도 배포다"** | **[E1]** Fastly 2021(정상 고객 설정이 잠복 버그 촉발 → **네트워크 85% 에러**, 감지 1분·49분 내 95% 복구) / CrowdStrike 2024(Channel File이 Validator 버그 통과 → 경계 밖 메모리 읽기 → 대량 BSOD) / Datadog 2023(자동 systemd 업데이트가 Cilium 라우트 삭제, **업데이트 창이 06:00 UTC로 동기화돼 5개 리전·3개 클라우드 동시 다운**). 셋 다 원문 확인 | 설정 1줄이 전 인스턴스를 크래시 루프에 빠뜨리는 것 재현 → **카나리·검증 게이트 유무 대비** | 쉬움(개념) |
| B45 ★★ | **스크립트 오류로 고객 데이터 삭제** | **[E1]** Atlassian 2022: 스크립트가 app ID 대신 **site ID를 받아 883개 사이트(775 고객) 영구 삭제**. 무검증 신뢰 + 소프트 딜리트 부재로 복구 최대 14일 (원문 확인) | 삭제 스크립트가 잘못된 스코프를 잡는 것 재현 → **dry-run·소프트딜리트·타입 검증·피어리뷰** 전후 | 쉬움 |
| B46 ★★ | **left-pad 2016 공급망 중단** | **[E1]** 11줄 패키지 포함 273개 언퍼블리시로 2.5시간 빌드 연쇄 실패, npm 정책 변경의 계기 (공식 포스트 확인) | Verdaccio에 의존 패키지 게시 후 삭제 → 하위 빌드 실패 → 락파일·벤더링·프록시 캐시 | 쉬움 |
| B19 ★★★ | **Knight Capital 플래그 재사용** | **[E1]** 45분에 4.6억 달러. 폐기 코드의 플래그를 신기능에 재사용 + 8대 중 1대 배포 누락 | 동일 서비스 8개 중 1개만 구버전 + 재사용 플래그 on → 그 1대만 폭주하는 미니 재현 | 쉬움 |
| B21 ★★ | **AWS S3 2017 오타 가드레일** | **[E1]** runbook 오타 하나로 인터넷 절반 4시간 다운 | "N대 제거" 스크립트의 상한·최소 잔존 검증 유무 비교 | 쉬움 |
| B22 ★★ | **Facebook 2021 자기 철회 연쇄** | **[E1]** BGP 철회로 자기 복구 도구까지 마비, 7시간 | 축소: "헬스체크 실패 시 스스로를 디스커버리에서 빼는" 로직이 연쇄 전멸을 만드는 시뮬레이션 | 중간 |
| B47 ★ | **Roblox 2021 73시간 장애** | **[E1]** Consul streaming 경합 + **BoltDB freelist가 삭제 페이지를 회수 못 해** 4.2GB 로그에 실데이터 489MB. 텔레메트리가 Consul에 순환 의존해 진단 54시간 지연 (원문 확인) | 전체 재현 비현실적. **"관측성이 장애 대상에 의존하면 눈이 먼다"** 순환 의존만 최소 데모(X4와 통합) | 어려움 |

### 3-9. 관측성·기타

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| B48 ★★★ | **Prometheus 라벨 카디널리티 폭발** | **[E2]** `user_id`를 라벨로 붙이면 시계열 폭증 → OOM. **customer_id 1만 값이면 카운터 1개가 1,200만 시계열**. Prometheus가 알림·오토스케일 임계경로면 눈덩이 | 요청마다 고유 라벨 방출 → `prometheus_tsdb_head_series` 급증 → relabel drop 비교 | 쉬움 |
| B14 ★★★ | **Cloudflare 2019 정규식 백트래킹** | **[E1]** 정규식 하나로 **27분 글로벌 다운, 트래픽 82% 급락**. CPU 보호장치가 리팩토링에서 제거돼 있었고 카나리 없이 전 세계 동시 배포 | 문제의 정규식을 미들웨어에 + 특정 페이로드 → 요청 1개가 CPU 100% → RE2 교체 비교. **10분 재현, 시리즈 대표 편** | 쉬움 |

---

## 4. 트랙 I — 인프라·SRE (11)

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| I01 ★★★ | **인증서 만료로 전면 장애** | **[E1]** MS Teams 2020(인증서 만료로 수백만 사용자 5시간), Ericsson 2018(O2 등 약 3,200만 가입자·보상 1.3억 달러) | 만료 임박 자체서명 인증서로 nginx TLS → 만료 순간 전 요청 실패 → 만료 감시 exporter + 자동 갱신 | 쉬움 |
| I02 ★★★ | **K8s CPU 스로틀링 (CFS 쿼터)** | **[E2]** limit 1 CPU면 100ms 주기당 100ms만 쓰고 강제 대기. **p99만 튀는 원인 1순위** | limit 건 파드에 부하 → `container_cpu_cfs_throttled_seconds_total` 증가와 p99 악화 → limit 상향·제거 비교 | 중간 |
| I03 ★★★ | **conntrack 테이블 고갈** | **[E2]** 차면 커널이 **조용히** 새 연결을 드롭. K8s 클러스터를 마비시킨 사례 공개 | conntrack_max 축소 + k6 대량 연결 → 무증상 드롭·간헐 타임아웃 → `nf_conntrack_count` 관측·튜닝 | 중간 |
| I04 ★★ | **에페메랄 포트·TIME_WAIT 고갈** | **[E2]** 고트래픽 리눅스의 절벽, 클라이언트 포트가 말라 연결 실패 | 포트 범위 축소 + 짧은 커넥션 폭주 → 연결 실패 → keep-alive·풀링·tw_reuse 비교 | 쉬움 |
| I05 ★★ | **컨테이너 메모리 한계와 JVM** | **[E2]** limit을 모르는 JVM이 호스트 기준으로 힙을 잡아 OOMKilled. `UseContainerSupport` 등장 배경 | 구/신 옵션으로 같은 limit에서 OOMKilled 여부 비교, exit 137·dmesg 확인 | 쉬움 |
| I06 ★★★ | **헬스체크 설계 실패로 무한 재시작** | **[E2]** liveness가 의존성까지 검사하면 DB 지연에 앱이 재시작 루프. K8s 공식 안티패턴 | liveness에 DB 체크 + DB 지연 → CrashLoopBackOff → liveness/readiness 분리·startupProbe | 쉬움 |
| I07 ★★★ | **롤링 업데이트 중 요청 유실** | **[E2]** preStop·graceful shutdown 없으면 in-flight 요청이 잘림. 무중단 배포의 실체 | 배포 중 k6 부하 → 5xx 재현 → preStop sleep+terminationGracePeriod+readiness 해제 순서로 **0건 만들기** | 쉬움 |
| I08 ★★ | **디스크 풀 (로그·WAL이 채운다)** | **[E2]** 디스크 100%면 DB가 쓰기를 멈추고 로그도 못 써져 원인 파악까지 마비 | 작은 볼륨에 로그·WAL 폭주 → 쓰기 정지 → 로테이션·아카이브·임계 알림 | 쉬움 |
| I09 ★★ | **DNS 캐시·TTL 함정** | **[E2]** JVM 무한 DNS 캐시로 페일오버 후에도 죽은 IP를 붙듦(RDS 단골) | 컨테이너 DNS를 바꿔도 앱이 구 IP 유지 → `networkaddress.cache.ttl` 전후 | 중간 |
| I10 ★★ | **cgroup·IOPS 제한과 DB** | **[E2]** 클라우드 볼륨 burst 소진 후 IOPS 절벽 | `--device-write-bps`로 IO를 조여 체크포인트·백업이 절벽 맞는 것 재현 | 중간 |
| I11 ★★★ | **Toyota 2023: 디스크 부족이 14개 공장을 세우다** | **[E1]** 공식 뉴스룸(2023-09-06)+로이터 확인. **정기 유지보수 중 DB 정리 작업이 디스크 용량 부족으로 에러 → 시스템 정지.** 백업 서버가 **같은 시스템 환경이라 동일 장애로 전환 실패**. 8/29부터 **14개 공장 28개 라인** 중단, 8/30 대용량 서버 이전 후 재개. 사이버 공격 아님을 명시 | 작은 볼륨에서 정리 배치가 임시 공간을 못 잡아 실패 + **백업 인스턴스도 같은 볼륨을 보게 구성해 동반 실패** → 백업 경로 물리 분리·용량 사전 검사 | 쉬움 |

---

## 5. 트랙 R — 클라우드 관리형 DB (18)

> **AWS 계정 없이 로컬 Docker 재현**을 1순위로 판정. 프리티어는 Single-AZ·20GB만 되고 **Aurora는 프리티어 자체가 없어서**, 관리형 기능은 오픈소스로 같은 메커니즘을 재현한다. `[L]`=로컬 재현, `[L-부분]`=메커니즘만.
> 각 편 말미에 **"관리형 기능이 무엇을 대신해 주는가"**를 한 문단 넣으면, 관리형 서비스가 대신해 주는 일을 원리로 이해하게 된다.

### 5-1. 페일오버·복제

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| R01 ★★★ | **비동기 복제본 승격 시 데이터 유실(RPO)** | **[E2]** 리드 리플리카는 비동기라 승격 시 ReplicaLag만큼 커밋이 사라진다고 AWS 문서가 직접 인정 | `[L]` 쓰기 중 primary kill → 승격 → **유실 트랜잭션 수 카운트** → 반동기로 유실 0 + TPS 손해 측정 | 중 |
| R02 ★★★ | **Multi-AZ 페일오버 DNS vs JVM 캐시** | **[E2]** 페일오버는 DNS 레코드 교체 방식. JVM이 호스트명을 사실상 영구 캐시해 **DB는 1분에 복구됐는데 앱만 몇 분 죽는** 전형 | `[L]` dnsmasq로 A레코드 전환, `networkaddress.cache.ttl` -1 vs 30 비교 | 중 |
| R03 ★★★ | **Aurora 리더 엔드포인트 쏠림** | **[E2]** 리더 엔드포인트는 **커넥션 단위 분산이지 쿼리 단위가 아님**(공식 문서 확인). HikariCP `maxLifetime`이 같으면 풀 전체 동시 재생성으로 한 대에 몰림(이슈 #1247) | `[L-부분]` 리플리카 3대 + DNS 라운드로빈, maxLifetime 지터 유무별 **커넥션 분포 그래프** | 중상 |
| R11 ★★ | **페일오버 후 콜드 버퍼 캐시** | **[E2]** 승격 인스턴스는 캐시가 비어 성능 급락. Aurora PG의 Cluster Cache Management가 이걸 푸는 기능 | `[L]` 재시작 직후 vs pg_prewarm 적용 후 p99. **R01과 한 편으로 묶기 좋음** | 중 |
| R15 ★★ | **계획 스위치오버 RPO 0 vs 강제 RPO>0** | **[E2]** Aurora Global DB는 계획 스위치오버만 RPO 0, **계획되지 않은 페일오버는 초 단위 유실**. "글로벌 DB면 무손실" 오해 깨기 | `[L-부분]` 네트워크 2개=리전 + netem 150ms → 계획 승격 vs 강제 kill 승격의 유실 건수 | 중상 |

### 5-2. 자원·설정 함정

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| R04 ★★★ | **비활성 복제 슬롯 WAL 폭증** | **[E2]** "CDC 파이프라인이 프로덕션 DB를 죽이는 가장 흔한 경로". 컨슈머가 멈춰도 슬롯이 열려 있으면 WAL 무한 누적 | `[L]` `pg_recvlogical` kill 후 쓰기 지속 → restart_lsn 정지·pg_wal 폭증 → `max_slot_wal_keep_size` 전후 | 중 |
| R05 ★★ | **스토리지 풀로 읽기 전용 전락** | **[E2]** 꽉 차면 쓰기 전면 실패. 복구도 함정(증설 최소 10%, **최대 6시간 재변경 불가**) | `[L]` 500MB loopback에 대량 INSERT → PG `PANIC: No space left` → 복구 절차(VACUUM FULL 공간 역설) | 하~중 |
| R06 ★★ | **커넥션 스톰 + 풀러 도입** | **[E2]** 배포·페일오버 직후 전 인스턴스 동시 재연결로 max_connections 고갈, 헬스체크까지 실패해 재시작 루프 | `[L]` max_connections=50에 앱 20개 동시 기동 → too many clients → 풀러·백오프 전후 | 하~중 |
| R07 ★★ | **RDS Proxy 커넥션 피닝** | **[E2]** prepared statement·임시 테이블·`SET`·**16KB 초과 SQL**이 피닝을 유발해 멀티플렉싱 무력화 | `[L-부분]` PgBouncer transaction 모드로 동일 개념 → "왜 트랜잭션 풀링에서 세션 상태를 쓰면 안 되는가" | 중상 |
| R08 ★★ | **파라미터 그룹 pending-reboot** | **[E2]** static 파라미터는 수동 리부트 전까지 미반영, **유지보수 윈도우에도 자동 재부팅 안 됨**. Terraform apply만으로 pending 전환 이슈 다수 | `[L]` MySQL `SET GLOBAL` 가능 여부·PG `pg_settings.context` 구분 실습 | 하 |
| R09 ★★ | **gp2 burst credit 고갈 IOPS 절벽** | **[E2]** 1GiB당 3 IOPS 베이스라인, 크레딧 0이면 I/O 큐잉으로 레이턴시 계단식 급등 | `[L]` `--device-write-iops` 100 제한 → sysbench p99 급변(리눅스 VM 권장) | 중 |
| R10 ★★ | **binlog 오버헤드 정량화** | **[E2]** AWS 공식: binlog 활성화 시 컴퓨트 오버헤드 최대 50%, enhanced binlog는 약 13%. Aurora는 내부 복제에 binlog를 안 씀 | `[L]` log_bin OFF/ON × sync_binlog × row_image 조합별 TPS 표 | 하 |
| R12 ★ | **Performance Insights 클론 만들기** | **[E2]** PI의 본질은 "DB Load = Active Sessions를 wait event로 분해". **2026-07-31 PI 대시보드가 CloudWatch DBI로 전환** 예정 | `[L-부분]` PG `wait_event_type`/MySQL `events_waits_*` 1초 샘플링 → Prometheus+Grafana로 PI 클론 | 상 |

### 5-3. 글로벌·라이브 도메인 (콘텐츠 플랫폼 대응)

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| R13 ★★★ **완료** | **[슬롯 카운터로 후원 카운터 핫 로우 해소](sessions/R13-slotted-counter/)** | **[E2]** 단일 행 카운터는 사실상 직렬화되고 데드락까지. PlanetScale **slotted counter 패턴**이 표준 해법 | `[L]` Spring Boot 9변형(JPA 조회후증가·JVM락·낙관·비관·원자적·슬롯16/64·Redis·파이프라인) 3회 반복. 처리량·p95·락 대기·정합성 동시 측정. 읽기 비용, 인스턴스 2대, Redis 장애 복구까지 | 중 |
| R14 ★★★ | **글로벌 서비스 문자셋·타임존 지뢰** | **[E2]** utf8mb3 이모지 실패, 인덱스 767바이트, **타임존 테이블이 설치는 되나 적재는 안 돼 `CONVERT_TZ()`가 NULL**, DATETIME vs TIMESTAMP의 DST 차이 | `[L]` 전부 로컬 재현. **난이도 최하인데 글로벌 플랫폼 적합도 최상 — 진입 편으로 적합** | 하 |
| R16 ★★ | **배치가 OLTP를 밀어내는 캐시 오염** | **[E2]** 정산 배치의 대량 스캔이 버퍼 풀을 밀어내 실시간 p99 붕괴. Aurora면 I/O 과금까지 | `[L]` sysbench + 대량 집계 동시 실행 p99 변화 → 리플리카 분리·work_mem 격리 | 중 |
| R17 ★★ | **시계열 이벤트 폭주와 파티션 운영** | **[E2]** 시청·채팅·선물 로그는 수 주면 수억 행. DELETE로 지우면 오히려 장애 | `[L]` 1억 행 DELETE vs DROP PARTITION의 시간·락·WAL량. **R04와 엮으면 "대량 DELETE가 WAL을 폭발시켜 슬롯 사고 유발" 연쇄 서사** | 중 |
| R18 ★ | **채팅을 RDB에 넣으면 안 되는 이유** | **[E2]** 저지연 고볼륨은 NoSQL·스트리밍이 표준 | `[L]` 초당 1만 INSERT를 RDB 직접 vs Redis Stream 버퍼 후 배치 플러시 | 중상 |

---

## 6. 트랙 S — 검색엔진 (12)

> Elasticsearch·Lucene 운영에서 반복되는 함정과 공개 장애를 다룬다.

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| S01 ★★★ | **매핑 폭발 + 동적 매핑 타입 충돌** | **[E1+E2]** Elastic이 전용 문서·블로그를 낼 만큼 흔함(기본 상한 1000필드) + Blinkit 실장애 포스트모템(HN 화제) | 키가 매번 바뀌는 JSON 1000건 색인 → `Limit of total fields exceeded` → dynamic strict·flattened·동적 템플릿 3안. 타입 충돌도 함께 | 쉬움 |
| S02 ★★★ | **딥 페이지네이션 (from+size 10000)** | **[E2]** `max_result_window` 기본 10000, 가장 먼저 만나는 벽. scroll 비권장 → search_after+PIT | 2만 건 색인 후 from=10000 → 에러 → search_after 전환. **tie-breaker 누락 시 문서 누락·중복까지 재현하면 깊이 급상승** | 쉬움 |
| S03 ★★★ | **디스크 워터마크 flood stage** | **[E2]** 95% 초과 시 `read_only_allow_delete`로 전환돼 쓰기 전면 차단, 디스크를 비워도 자동 해제 안 되던 구버전으로 악명 | 워터마크를 절대값(flood 200mb)으로 낮추고 색인 → 쓰기 차단 → 해제·ILM 설계 | 쉬움 |
| S04 ★★★ | **bulk 리젝션** | **[E2]** 색인 파이프라인의 필수 관문. write 큐 포화 시 **조용히 문서 유실** | `--cpus=1` + 병렬 bulk → `_cat/thread_pool` rejected 관측 → 백오프·streaming_bulk 전후 유실 건수 | 쉬움 |
| S05 ★★★ | **Nori 함정 + 분석기 불일치** | **[E2]** ES 6.6부터 공식 한글 분석기. "홍대입구"가 홍대+입구로 쪼개져 엉뚱한 역이 검색되는 사례가 정설 | `_analyze`로 decompound_mode 비교, analyzer≠search_analyzer 시 결과 붕괴. **한국어 형태소 분석의 실무 함정** | 쉬움 |
| S06 ★★ | **서킷 브레이커 + fielddata OOM** | **[E2]** `[parent] Data too large`는 ES 운영 질의 최상위. text에 fielddata 켜고 집계하면 힙 상주 | 힙 512MB + text fielddata 집계 → circuit_breaking_exception → keyword 전환 | 쉬움 |
| S07 ★★ | **오버샤딩과 샤드 상한** | **[E2]** 프라이머리당 10~50GB 권장, `max_shards_per_node` 1000 초과 시 인덱스 생성 차단 | 샤드 과다 인덱스 반복 생성 → validation_exception → 샤드 1개 vs 50개 검색 지연 비교 | 쉬움 |
| S08 ★★ | **값비싼 쿼리(wildcard·regexp)** | **[E2]** CPU 스파이크 반복 원인이라 Elastic이 `allow_expensive_queries` 스위치를 따로 둠 | 수십만 건에 `*abc*`·백트래킹 regexp → CPU·p99 → 차단 스위치·edge_ngram 대안 | 쉬움 |
| S09 ★★ | **무중단 reindex + alias 원자 전환** | **[E2]** 앱이 인덱스 실명을 참조하면 무중단 전환 원천 불가, 재색인 중 쓰기 처리는 공식 문서도 안 다룸 | 쓰기 부하 중 `_reindex` → `_aliases` 원자 전환 → **재색인 중 쓰기 유실 건수**가 핵심 지표 | 중간 |
| S10 ★★ | **refresh_interval·translog** | **[E2]** Elastic 공식 튜닝 1순위. 벌크 중 refresh를 끄면 2~5배 | refresh 1s/30s/-1 3회 색인 비교 + "방금 넣은 문서가 안 보이는" 부작용까지 | 쉬움 |
| S11 ★ | **Lucene 세그먼트 머지 폭풍** | **[E2]** ES가 Lucene의 색인 스로틀 보호를 끈 탓에 머지가 밀리면 세그먼트 폭증 | refresh 짧게 + 대량 색인 + max_merge_count 축소 → 스로틀 로그·`_cat/segments` → force merge 전후 | 중간 |
| S12 ★ | **GitHub 2013 코드 검색 스플릿 브레인** | **[E1·축소]** ES 0.19→0.20 업그레이드 중 `minimum_master_nodes` 미설정으로 **510샤드 중 7샤드 영구 유실** (원문 확인) | ES 6.x 이하 구버전 3노드 + disconnect. **7.0에서 설정 자체가 제거돼 구버전 이미지 필수** | 어려움 |

---

## 7. 트랙 V — 벡터DB·RAG (8)

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| V01 ★★★ | **필터 + ANN의 recall 붕괴** | **[E2]** pgvector README 원문: "조건이 10% 행에 매치하면 기본 ef_search 40에서 **평균 4행만 매치**". Weaviate ACORN 벤치마크상 선택도 20%가 변곡점 | 10만 벡터 + 카테고리 필터 → 결과 부족·recall 하락 → **브루트포스 정답셋으로 recall@10 정량화** → `hnsw.iterative_scan` 전후 | 쉬움 |
| V02 ★★★ | **HNSW 파라미터 스윕** | **[E2]** pgvector 기본값 m=16·ef_construction=64·ef_search=40, IVFFlat probes=1이 "검색 품질 왜 이래"의 최다 원인 | 파라미터 스윕 → **recall@10 vs p95 vs 빌드시간 3축 곡선** | 쉬움 |
| V03 ★★★ | **연산자 클래스 불일치로 인덱스 무시** | **[E2]** `vector_cosine_ops`로 만들고 `<->`로 조회하면 인덱스를 조용히 무시하고 Seq Scan + 정렬 기준까지 바뀜 | 5분 재현, EXPLAIN ANALYZE에 Seq Scan 증거 | 아주 쉬움 |
| V04 ★★ | **거리 메트릭·정규화 실수** | **[E2]** L2 정규화하면 코사인·내적·유클리드가 같은 순위. 정규화를 빼먹었을 때만 메트릭이 결과를 바꿈 | 정규화 2 × 메트릭 3 조합의 top-k 순위 일치도 표 | 아주 쉬움 |
| V05 ★★ | **인덱스 빌드 메모리 폭발** | **[E2]** pgvector README: `maintenance_work_mem` 초과 시 10만 튜플부터 NOTICE, 디스크 폴백으로 극단 저하 | 64MB로 100만 벡터 빌드 → NOTICE → 2GB 상향 비교 | 쉬움 |
| V06 ★★ | **하이브리드 점수 결합·RRF** | **[E2]** BM25는 상한이 없고 코사인은 [-1,1]이라 원점수 합산 시 BM25가 지배. RRF는 튜닝 불필요 | BM25 단독 / kNN 단독 / RRF 3안의 nDCG@10 비교. **검색·벡터를 한 편에 묶는 마무리 글** | 쉬움 |
| V07 ★ | **벡터 인덱스가 메모리에 안 올라갈 때** | **[E2]** HNSW 그래프는 RAM 상주 전제, 넘치면 p99 급등. 빌리언 스케일에선 PQ 압축이 필수가 되고 recall 대가 | `--memory=1g`에서 대용량 검색 → p99 급등 → on_disk·양자화 비교 | 중간 |
| V08 ★ | **업서트·삭제 tombstone 누적** | **[E2]** HNSW는 삭제를 직접 지원하지 않아 소프트 삭제+주기 compaction, 그 사이 그래프 품질 저하 | 대량 upsert/delete 반복 → 디스크·세그먼트·recall·p99 시계열 | 중간 |

> 재현 불가(사례 분석용): **Pinecone 2023-03-01** 정리 스크립트 SQL 버그로 무료 인덱스 515개 삭제(원문 확인) → "정리 배치의 안전장치 설계" 관점으로 B45와 묶어 서술

---

## 8. 트랙 L — LLM·RAG 운영 (10)

> 벡터 트랙 V가 "검색 품질"이라면 이 트랙은 **서빙·에이전트·비용·보안 운영**을 다룬다.

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| L01 ★★★ | **에이전트 무한 루프·토큰 예산 폭주** | **[E2]** 피드백 경로가 안 묶이면 한 요청이 도구 호출을 무한 증폭. arXiv에 **63건의 실제 예산 초과 사고 카탈로그**(달러 손실 문서화)와 "Infinite Agentic Loops" 논문 존재 | 종료 조건을 느슨하게 주고 순환 유발 → 비용 폭주 그래프 → **스텝 상한·토큰 예산·루프 감지·킬 스위치** 4종 비교 | 쉬움 |
| L02 ★★★ | **RAG 간접 프롬프트 인젝션** | **[E2]** OWASP LLM Top 10의 **LLM01(최다 악용)**. 지식베이스 문서 하나가 오염되면 그걸 검색하는 **모든 사용자**가 감염 | 문서에 지시문을 심고 검색되게 만들어 시스템 프롬프트 우회 재현 → 문서 격리 태깅·출력 스키마 강제·도구 화이트리스트 | 쉬움 |
| L03 ★★★ | **vLLM KV 캐시 포화와 선점** | **[E2]** GPU 캐시 90% 초과 시 시퀀스 선점 시작, 지속 선점은 정상 운영이 아닌 성능 저하 상태. swap 모드는 긴 컨텍스트 복원에 수백 ms | GPU 없으면 CPU 백엔드·작은 모델로 축소: 동시 요청 증가 → `num_preemptions_total`·p99 → max_len·utilization 비교 | 중간 |
| L04 ★★ | **컨텍스트 윈도 초과와 조용한 절단** | **[E2]** 문서를 욱여넣다 한도를 넘기면 앞부분이 잘려 "분명 넣었는데 모델이 못 본다" | 토큰 한도를 낮추고 문서 수 증가 → 정확도 붕괴 → 재랭킹·압축·문서 수 상한 | 쉬움 |
| L05 ★★ | **RAG 청킹과 recall 분해 측정** | **[E2]** "답이 틀리면 프롬프트부터 고친다"가 함정. **검색 실패 vs 생성 실패를 분리 측정**해야 함 | 청크 크기·오버랩 스윕 → recall@k와 답변 정확도를 분리 측정 → 최적 구간 곡선 | 쉬움 |
| L06 ★★ | **LLM 응답 스키마 강제 실패** | **[E2]** 모델이 JSON을 한 번 어기면 백엔드는 그대로 500. 재시도 루프가 L01로 번짐 | 스키마 위반 유도 → 파싱 실패 → function calling·JSON 모드·GBNF·재시도 상한 비교 | 쉬움 |
| L07 ★★ | **SSE 스트리밍 끊김** | **[E2]** 프록시·LB 버퍼링·유휴 타임아웃에 걸려 조용히 끊김(M11과 같은 뿌리) | nginx 버퍼링 on/off, idle timeout 축소 → 중단 재현 → 하트비트·재연결·부분 응답 복구 | 쉬움 |
| L08 ★★ | **동시 요청 큐잉과 p99** | **[E2]** 연속 배칭은 처리량을 올리지만 대기 큐가 길어지면 개별 p99는 악화. TTFT와 총 지연의 트레이드오프 | 동시성 스윕에서 TTFT·총 지연·처리량 3축 → 큐 상한·load shedding·우선순위 큐 | 중간 |
| L09 ★ | **온프레미스 모델 서빙 최적화** | **[E2]** AX 직무의 "온프레·프라이빗 AI 실행 환경" 그 자체. 양자화로 메모리를 줄이는 대신 품질 저하 | fp16 / 8bit / 4bit 메모리·지연·품질 3축 비교. GPU 없으면 llama.cpp CPU | 중간 |
| L10 ★ | **LLM-as-a-Judge 편향** | **[E2]** 자체 평가가 자기 모델을 편애하거나 위치·길이 편향. 평가가 틀리면 개선 방향 전체가 틀어짐 | 순서 뒤집기·길이 통제로 편향 측정 → 앵커·루브릭·다수결 전후. 쌍별 평가 데이터 부트스트랩이 선행 작업 | 중간 |

> **이 트랙이 다루지 않는 것**: 그래프 DB, LLMOps 툴체인(MLflow·LangSmith), 파인튜닝. GPU 서빙은 L03·L09에서 부분적으로만 다룬다

---

## 9. 트랙 M — 모바일·앱웹 연동 (14)

> 관점은 **"백엔드가 모바일을 상대할 때 겪는 문제"**. 서버는 배포하면 즉시 반영되지만 앱은 사용자가 업데이트해야 바뀐다는 비대칭이 이 트랙을 관통한다.
> **묶음 추천**: M01 → M02 → M05 → M06이 하나의 이야기("푸시로 서버가 죽었다 → 재시도가 중복 주문을 만들었다 → 이중 결제로 번졌다 → 오프라인 큐가 증폭시켰다"). **단일 데모(주문 서비스+푸시 워커+부하 주입기)로 4~5편** — 구축 비용 대비 산출 최고.

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| M01 ★★★ | **푸시 발송 직후 동시 접속 폭주** | **[E2]** 푸시의 진짜 부하는 전송이 아니라 "전원이 동시에 앱을 여는 것". FCM 공식 문서가 정각 발송 시 첫 30초~2분 트래픽 2배 이상을 경고하며 해당 윈도 회피 권고(원문 확인) | k6로 "0초에 5만 VU 동시 진입" → 캐시 미스·풀 고갈·재시도 증폭 → 지터 분산·스탬피드 방지·load shedding 3안 | 쉬움 |
| M02 ★★★ | **멱등성 없는 재시도로 중복 주문** | **[E2]** 모바일 백엔드 실무 빈출 1위. 앱 타임아웃 재시도 = 서버는 이미 처리함 | Toxiproxy 5초 지연 + 클라 2초 타임아웃 → 중복 100% → **순진한 멱등키가 동시 요청에서 다시 깨지는 것까지** → 유니크 제약·락으로 완성 | 쉬움 |
| M03 ★★★ | **리프레시 토큰 회전 레이스** | **[E2]** 진입 시 API 5개가 동시에 401 → 같은 토큰으로 동시 갱신 → RTR 재사용 탐지가 패밀리 전체 무효화 → **강제 로그아웃 대란**. Auth0류가 60초 유예를 두는 이유 | 만료 토큰으로 동시 요청 10개 → 결정론적 재현 → 클라 뮤텍스·서버 유예·분산 락 3안 | 쉬움 |
| M04 ★★★ | **인증서 피닝 + 로테이션** | **[E2]** 백엔드가 인증서만 갱신했는데 **모든 버전 앱이 동시에** 끊기고 앱 업데이트 전까지 복구 불가. Cloudflare가 2024 Q3만으로 2023년 3개 분기 합계 수준의 피닝 장애 신고를 받았다고 공개(원문 확인) | nginx + 자체서명 A로 핀 고정 → 인증서 B 교체 → 전 요청 실패 → 백업 핀·동적 피닝 | 쉬움 |
| M05 ★★★ | **IAP 영수증 검증·환불 웹훅 누락** | **[E2]** 검증과 지급 사이가 끊기면 돈은 냈는데 재화가 없고, 재시도가 멱등하지 않으면 이중 지급. 환불 주체가 Apple/Google이라 웹훅이 없으면 **환불된 사용자가 재화를 계속 사용** | **가짜 스토어 컨테이너**(검증 API + 환불 웹훅 + 지연·중복·순서역전 주입) → 중복 지급 방지·리컨실 배치·순서 역전 방어 | 중간 |
| M06 ★★ | **오프라인 큐 flush storm** | **[E2]** 복구 즉시 쌓인 작업을 전부 밀어넣음. 무차별 재시도가 0.5초에 수만 번으로 SMS 플랫폼을 압도한 사례가 MS 안티패턴 문서에 기록 | 클라 500개 × 작업 20개 축적 → down 해제 순간 동시 flush → 백오프+지터·배출 제한·Retry-After·배치 API | 쉬움 |
| M07 ★★ | **API 계약 파괴** | **[E2]** 구버전 앱은 강제 업데이트 전까지 영원히 남는다. Google AIP-180이 하위 호환을 공식 규범화 | 구/신 DTO로 breaking change → **CI에서 JSON Schema diff·컨트랙트 테스트로 차단하는 파이프라인**이 산출물 | 쉬움 |
| M08 ★★ | **강제 업데이트·kill switch 부재** | **[E2]** Remote Config 의존 시 그게 죽으면 앱 진입 불가, 스토어 노출 전 플래그를 켜면 업데이트 대상 없이 잠김. iOS는 심사 때문에 롤백이 5분이 아님 | 버전 정책 API 죽여서 부팅 불가 재현 → fail-open·캐시된 정책·타임아웃 방어 → 서버 플래그로 기능 끄기 30초 데모 | 쉬움 |
| M09 ★★ | **대량 발송 병렬화·Redis 파티셔닝** | **[E1]** 국내 공개 수치: 1,053만 명에 첫 발송 6분·마지막 11분 → **1,124만 명에 첫 1초·마지막 51초**. 비동기화 + 1만 파티션 분산(Redis 싱글스레드 병목 해소) | 단일 키 vs `user % 10000` 파티션의 SISMEMBER/SADD 처리량 비교 | 중간 |
| M10 ★★ | **무효·좀비 토큰 누적** | **[E2]** Firebase가 2024-05-15부터 270일 미사용 토큰 정리. 정리 로직 없으면 발송 대상 상당수가 영구 실패로 쿼터만 소모 | 유효 1만 + 무효 9만 토큰, 목이 404 UNREGISTERED → 정리 배치 유무로 완료 시간·성공률 | 쉬움 |
| M11 ★★ | **백그라운드 전환·좀비 소켓** | **[E2]** WebSocket에 내장 keep-alive 없음, 캐리어 NAT 유휴 타임아웃 흔히 30초. 백그라운드에선 하트비트도 멈춰 "서버만 살아있다고 믿는" 유령 연결 누적 | 클라 1000개 → 프록시 down/up → 좀비 연결·FD·메모리 누적 → idle timeout + ping 강제 종료 | 중간 |
| M12 ★★ | **Firebase 과금 폭탄과 킬 스위치** | **[E1]** 전직 구글러가 무한 재귀로 하룻밤 **1,160억 읽기 $72,000** 청구. Blaze에 하드 스펜딩 캡이 없고 예산 알림은 차단이 아님 | **로컬 에뮬레이터로 무과금 재현**(트리거 재귀) → 재귀 차단·최대 인스턴스 제한·예산 Pub/Sub 킬 스위치 | 쉬움 |
| M13 ★ | **Android App Links 검증 실패** | **[E2]** Apple은 AASA를 **리다이렉트 비활성으로** 가져가므로 서버가 301을 주면 유니버설 링크가 통째로 죽음 | Android로 축소: `assetlinks.json`을 301 뒤에 두거나 Content-Type 오류 → `adb shell pm verify-app-links` 출력 차이 | 중간 |
| M14 ★ | **다중 기기 세션·푸시 토큰** | **[E2]** 단일 IP·기기 지문 기반 세션은 기기 교체 시 붕괴. 푸시 토큰도 기기 단위라 교체 시 중복 알림 | 기기 A/B/C → "다른 기기 로그아웃"·중복 푸시 검증 → 기기 단위 세션 목록·선택 무효화 | 쉬움 |

---

## 10. 트랙 F — 금융·증권 IT (21)

> 전문·원장·정산·대사처럼 금융 시스템 고유의 정합성 요구를 다룬다.

### 10-1. 국내 실제 사고

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| F01 ★★★ | **한맥 2013: 0으로 나눈 값이 검증을 뚫다** | **[E1]** 이자율을 `잔여일/365` 대신 `잔여일/0`으로 입력 → 호가 상한·하한 미설정 → **143초에 37,900건 체결, 463억 손실, 회사 파산**. 대법원은 거래 취소 불허(판결 확인) | `1.0/0.0`이 예외가 아니라 `Infinity`가 되고 `price <= upperLimit`를 전부 통과하는 것 재현 → 주문 루프 → NaN/Infinity 가드·초당 제한·킬스위치 | 쉬움 |
| F02 ★★★ | **삼성증권 2018: 불변식 부재** | **[E1]** 배당 1,000원 대신 **자사주 28.1억주** 착오 입고(발행총수 초과인데 원장이 수용), 직원 22명이 31분간 매도 → 주가 -11.7%. 금융위 업무 일부정지 6월(검사결과 원문 확인) | 금액·수량 단위 혼선 재현 → **불변식(발행총수 ≥ 잔고 합계) 없이 커밋 성공** → CHECK/트리거 + 임계 초과 시 maker-checker | 쉬움 |
| F03 ★★★ | **장 시작 9시 접속 폭주** | **[E1]** 2022~2026.3 증권 MTS 장애 **190건**(카카오페이증권 42·토스증권 42로 44%). 한국투자증권 2026-05-06 개장 직후 7분, 메리츠 2026-04-09 약 40분 | k6로 9시 정각 스파이크 → 풀 고갈·큐 적체 → **가상 대기열·load shedding·조회와 주문 경로 분리** | 중간 |
| F04 ★★ | **주문가능금액 오표시(캐시 vs 원장)** | **[E1]** 토스증권 2026-02 오표시("증거금 시스템 일시 오류"), 한국투자증권 2026-03·06 퇴직연금 잔고·수익률 오표기(잘못된 수익률로 매도해 손실 본 투자자 발생) | 캐시 무효화 누락·비동기 갱신이 원장 커밋보다 먼저 반영되는 순서 역전 → **캐시 키에 원장 버전 포함** | 중간 |
| F05 ★★ | **평가금액 스냅샷 불일치** | **[E2]** 평가금액 = 수량 × 현재가인데 **수량 스냅샷과 시세 스냅샷을 다른 시점에 읽으면** 화면 금액이 틀어짐 | 두 소스를 다른 시각에 조회 → 불일치 → as-of 시점 고정·단일 트랜잭션 스냅샷 | 중간 |
| F06 ★ | **복수 시장(ATS) 라우팅과 일괄 취소** | **[E1]** 넥스트레이드 2025-03 출범 직후 "주문은 넣었는데 체결 안 된다". **프리마켓 종료 후 미체결 일괄 취소가 전산 과부하·증거금 해제 곤란으로 매우 어렵다** | KRX·ATS 모사 엔진 2개 + SOR → 시장 전환 시 일괄 취소와 증거금 해제 경합 | 중간 |

### 10-2. 해외 금융 사고

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| F07 ★★★ | **Nasdaq 2012 FB IPO 재계산 라이브락** | **[E1]** 크로스 계산이 끝나고 기록 직전 주문 정정이 계속 들어와 **무한 재계산 루프**. 11:05 시작이 11:30에야 보조 엔진 전환, **38,000건 이상 미포함**, SEC 벌금 1,000만 달러(공식 공지 확인) | 단일가 계산기에 "기록 직전 변경 시 재계산" 규칙 그대로 구현 → 유입률 > 계산시간이면 **영원히 확정 안 됨** → **스냅샷 동결 + 접수 컷오프** | 중간 |
| F08 ★★ | **Nasdaq 2013 flash freeze** | **[E1]** SIP 용량 포트당 초당 1만인데 NYSE Arca 재연결 반복으로 **포트당 초당 2.6만(평시 26배)** + 잘못된 심볼 거부 폭주 → 3시간 이상 거래 중단 | 시세 서버 + 정상 소비자 N + 재접속 루프 1 → **거부 경로가 정상 경로와 큐를 공유할 때 전체 마비** → 격리 큐·서킷브레이커 | 중간 |
| F09 ★★ | **IB 2020 음수 유가** | **[E1]** WTI **-37.63달러** 마감인데 시스템이 음수 가격을 상정하지 않아 화면 미표시·음수 지정가 불가·증거금 오적용. CFTC 과징금 175만 + 배상 8,257만 달러 | `CHECK (price > 0)` 또는 부호 미처리 파서 → 음수 틱에서 파싱 실패·미표시·증거금 부호 역전 연쇄 | 쉬움 |
| F10 ★★ | **NYSE 2015 설정 드리프트** | **[E1]** 새 릴리스를 한 거래 유닛에만 배포하고 게이트웨이 설정은 안 맞춰 통신 실패, **3시간 38분 중단(NYSE 최장)** | 게이트웨이 2대 중 1대만 새 프로토콜 → 버전 협상 실패로 특정 클라이언트만 접속 불가 → 드리프트 감지·하위 호환 협상 | 쉬움 |
| F11 ★ | **TSB 2018 마이그레이션 실패** | **[E1·축소]** 540만 고객·13억 레코드 이관. **두 데이터센터 설정 불일치가 이관 전에 발견되지 않음**. FCA는 "견고한 회귀 테스팅 부재"를 근본 원인으로 명시. 손실 1.54억 파운드·벌금 4,800만 파운드 | MySQL→PG 100만 건 이관, **컨테이너 그룹 A/B에 타임존·문자셋·풀 크기 중 하나만 다르게** → 트래픽 절반만 실패 → 대사 배치가 잡아내게 | 중간 |
| F12 ★ | **Monzo 2019 auto_bootstrap 오용** | **[E1]** 새 Cassandra 노드가 **데이터 없이 소유권만 가져가** 조회가 "row not found". 개선책으로 런북·지표 알림 추가 | Cassandra 3노드에 `auto_bootstrap=false` 노드 추가로 실제 재현(메모리 부담 큼) | 중간 |

### 10-3. 증권 도메인 고유 과제

| # | 문제 | 근거·출처 | 재현 | 난이도 |
|---|---|---|---|---|
| F13 ★★★ | **가격우선·시간우선 매칭엔진 동시성** | **[E2]** 같은 가격이면 먼저 접수된 주문 우선, 한국 주식은 1/1000초 단위 접수라 대부분이 이 두 원칙으로 결정 | 동일 가격 주문 병렬 투입 → 접수 순서와 체결 순서 어긋남 → tie-break·시퀀서·단일 스레드 매칭 비교. **증권 매매 시스템의 핵심 규칙** | 쉬움 |
| F14 ★★ | **정정 시 시간우선순위 규칙** | **[E2]** **수량 감소 정정은 우선순위 유지·가격 변경이나 수량 증가는 상실**(증권사 약관 확인) | 규칙을 오더북에 구현 → 오구현 시 특정 참가자가 부당하게 앞자리를 유지하는 시나리오를 테스트로 고정 | 쉬움 |
| F15 ★★★ | **WebSocket 시세 슬로우 컨슈머** | **[E2]** 백프레셔가 없으면 느린 클라이언트 하나로 송신 버퍼가 무한 증가, WebSocket 서버 OOM의 대표 원인 | 정상 클라 N + sleep 클라 1 + 초당 수천 틱 → 힙 증가·OOM → **바운디드 큐 + conflation(종목별 최신 틱만) + 느린 소비자 절단** | 쉬움 |
| F16 ★★ | **FIX 시퀀스 갭과 재전송 중복** | **[E2]** MsgSeqNum이 기대보다 크면 유실로 보고 복구. 관리 메시지는 `SequenceReset(GapFillFlag=Y)`. **PossDupFlag를 무시하면 주문 중복 접수** | QuickFIX/J 2컨테이너 → 세션 절단으로 갭 → ResendRequest·GapFill 검증 → ClOrdID 중복 제거. **FIX 세션 계층을 직접 다루는 드문 소재** | 중간 |
| F17 ★★ | **부동소수점 금지(BigDecimal)** | **[E1]** Vancouver 1982 절삭 사건이 역사적 근거 | `double` vs `numeric(19,4)`로 100만 건 누적 합계 차이, 수수료·거래세 반올림(HALF_UP vs HALF_EVEN), `BigDecimal.equals` vs `compareTo` scale 함정. **금융 도메인의 기본 전제** | 하 |
| F18 ★★ | **영업일·휴장일·서머타임** | **[E2]** 미국 주식 T+1 결제가 미 공휴일이면 순연, 콜럼버스 데이·베테랑 데이는 증시는 열리나 채권시장 휴장이라 결제 불가, DST 전환 주간 개장시간 1시간 이동 | 컨테이너 TZ=UTC + 앱은 Asia/Seoul 가정 → 결제일이 하루 밀리는 것 재현 | 하 |
| F19 ★ | **마감(cutoff) 경계** | **[E2]** 서버 두 대 시계가 수백 ms 어긋나면 한 대는 마감 후 주문을 수용 | 중앙 시퀀서 시각 단일 기준으로 해결. **구체적 국내 사고 사례는 없으므로 "설계 과제"로만 서술** | 쉬움 |
| F20 ★★ | **규제 대응 DR** | **[E2]** 전자금융감독규정 제23조: **"핵심업무의 복구목표시간은 3시간 이내, 보험회사는 24시간 이내"**(조문 확인). 제11·36조는 전산실·재해복구센터 보안성심의, 국내 설치 의무 | 주/부 사이트 컨테이너 강제 종료 후 **실제 전환 시간을 재 RTO를 수치로** 제시. 로그 보존은 append-only+해시 체인, 망분리는 network 분리로 | 중간 |
| F21 ★ | **시퀀스 채번 병목** | **[E2]** 단조증가 인덱스의 **우측 리프 블록 경합**(RAC에선 인스턴스 간 이동으로 악화). Reverse Key·해시 파티션·큰 캐시+NOORDER | 주문번호 대량 INSERT의 리프 경합 → 해시 프리픽스·파티션 분산. **B06(카운터 행 락)과 겹치지 않게 "인덱스 리프 경합" 각도로** | 중간 |

---

## 11. 트랙 X — 축소 재현 설계 (4)

> 사건은 실재하지만 물리·관리형 인프라가 원인이라 그대로는 불가. **각 편 서두에 축소 재현임을 반드시 명시한다.**

| # | 원 사건 | 축소 재현 설계 | 난이도 |
|---|---|---|---|
| X1 ★ | **Stripe 2019** — 헬스체크는 통과하지만 죽은 노드 + 선출 실패 | MongoDB replica set 3노드에서 한 노드의 **복제 적용만 멈추고 ping은 살려두는** 상태(`fsyncLock` 또는 tc로 oplog만 지연) → "헬스체크 통과하지만 실제로는 죽은 노드" 괴리 → priority/votes 충돌로 선출 실패까지 | 어려움 |
| X2 ★ | **Slack 2021** — AWS TGW 스케일링 실패 + open file limit | 게이트웨이(nginx/HAProxy)에 연결 수 상한을 걸어 스케일링 지연 모사 + 트래픽 급증 시 ulimit 고갈 재현 | 중간 |
| X3 ★★ | **Cloudflare 2023** — HA가 일부에만 적용된 시스템 | 서비스 A는 2노드 이중화, A가 의존하는 B는 단일 인스턴스 → B를 docker stop → **A가 이중화돼 있어도 전체 마비**. 의존성 그래프에서 HA의 최소 컷 찾기가 해결 편 | 쉬움 |
| X4 ★★ | **카카오 1015 (2022)** — DC 편중 + 복구 도구 동반 사망 | docker network 2개를 DC로 보고 이중화가 한쪽에 편중 + 모니터링·배포 스크립트도 같은 네트워크 → `network disconnect` → 페일오버 실패와 **복구 도구도 같이 죽는 상태** 재현. **관측·복구 도구를 장애 대상과 물리적으로 분리해야 하는 이유**(B47 Roblox 순환 의존과 통합 가능) | 중간 |

---

## 12. 근거 검증 로그 (2026-07-28 브라우저 직접 확인)

### 확인 완료 → 등급 승격
- **Toyota 2023 [E4 → E1, 신규 세션 I11]**: 공식 뉴스룸 "Concerning the production order system malfunction"(2023-09-06) + 로이터(09-05) 확인. 정기 유지보수 중 DB 정리가 디스크 부족으로 실패 → 시스템 정지, **백업 서버가 같은 환경이라 동반 실패**, 14개 공장 28개 라인 중단, 익일 대용량 서버 이전 후 재개, 사이버 공격 아님
- **전자금융감독규정 RTO [E4 → E2, F20 해제]**: 제23조 **"핵심업무 복구목표시간 3시간 이내, 보험회사 24시간 이내"** 조문 확인. AWS 금융 클라우드 가이드도 동일 인용
- **ForkJoinPool commonPool [E4 → E2, B32에서 분리해 B51]**: Baeldung이 commonPool 공유를 명시 + **국내 실장애 회고 "parallelStream 남용으로 인한 장애 경험기"(2019)** 확인

### 부분 확인 → 등급 유지
- **파일 디스크립터 누수 [E2, B32]**: 진단 가이드 다수 확인(원인·`/proc/[PID]/fd`·lsof·ulimit). **대표 단일 사건은 없으므로 "○○사 사건"이라고 쓰지 말 것**

### 확인 실패 → 폐기·강등
- **동기 로깅·로그 폭주 디스크 풀 [B50 폐기]**: 대표 공개 사례를 못 찾았고, 오히려 "동기 로깅의 성능 영향은 미미해 비동기 전환 이득이 거의 없었다"는 반대 실측도 존재. **근거가 갈리므로 카탈로그에서 제거**(디스크 풀 자체는 I08·I11로 충분)
- **코스콤 PowerBASE 장애 사례**: 확인 실패 → 언급 금지
- **국내 증권사 EOD 배치 실패 사례**: 확인 실패("EOD"가 국내 기사에선 주로 Event of Default) → F 트랙의 EOD는 **사건 인용 없이 설계 과제로만**
- **토스·카카오페이증권 자체 장애 포스트모템**: 기술블로그에 장애 회고 확인 실패 → **F03은 언론 보도 통계로만** 인용
- **수치 인용 금지 목록**: Apple `ThunderingHerdPolicy` 발동 조건 · 긴급 심사 24h 보장 · Android Vitals 5~10% 실패율 · Twitch DB 대수 · Aurora DSQL 커밋 레이턴시 · RDS Single-AZ 백업 중 I/O 중단 · 리더 엔드포인트 DNS TTL · pgvector "600배" · Blinkit 포스트모템 본문(403) · 쿠팡 2025-11 검색 장애 원인(비공개)
- **AWS us-east-1 2025-10 공식 포스트모템에 RDS/Aurora는 명시되지 않음** — 2차 매체의 "RDS도 영향" 서술을 인용하면 안 됨

### 보안 세션 주의
B38(Log4Shell)·B40(SSRF)·B41(역직렬화)·B42(xz)는 **격리된 로컬 환경에서만**. 실제 익스플로잇 공개나 외부 대상 테스트 금지. xz는 실 백도어 재현 자체를 하지 않는다.

---

## 13. 진행 체크리스트 (169개, 전 세션 미시작)

**A 트랙 (21)**
- [ ] A01 정수 PK 고갈 · [ ] A02 MDL 폭풍 · [ ] A03 utf8mb4 · [ ] A04 락 에스컬레이션 · [ ] A05 커넥션 폭풍 · [ ] A06 갭 락 · [ ] A07 롱 트랜잭션 · [ ] A08 buffer pool · [ ] A09 통계 오염 · [ ] A10 복제 지연 · [ ] A11 세미싱크 유실
- [ ] A12 GitHub 스플릿브레인 · [ ] A13 GitLab 재구축 · [ ] A14 XID · [ ] A15 fsyncgate · [ ] A16 OOM Killer · [ ] A17 UUID 스플릿 · [ ] A18 Uber 쓰기 증폭 · [ ] A19 SLRU 절벽 · [ ] A20 CRDB 쿼럼 · [ ] A21 Figma 논리복제

**B 트랙 (50)**
- [ ] B01 HikariCP · [ ] B02 스탬피드 · [ ] B03 read-your-writes · [ ] B04 페이지네이션 · [ ] B05 리트라이 폭풍 · [ ] B06 핫 로우 · [ ] B07 coalescing · [ ] B08 GC 스톨 · [ ] B09 Kafka 리밸런스 · [ ] B10 클럭 스큐 · [ ] B11 Redis 커넥션 · [ ] B12 memcache lease · [ ] B13 샤딩
- [ ] B14 정규식 백트래킹 · [ ] B15 ORDER BY RAND · [ ] B16 Soft Delete · [ ] B17 FLOAT 돈 계산 · [ ] B18 DST · [ ] B19 Knight 플래그 · [ ] B20 500마일 · [ ] B21 S3 가드레일 · [ ] B22 자기 철회 · [ ] B23 테넌트 격리
- [ ] B24 분산락 펜싱 · [ ] B25 self-invocation · [ ] B26 트랜잭션 내 외부호출 · [ ] B27 exactly-once · [ ] B28 poison DLQ · [ ] B29 스케줄러 중복 · [ ] B30 무한큐 OOM · [ ] B31 ThreadLocal 누수 · [ ] B32 FD 누수 · [ ] B51 commonPool 오염
- [ ] B33 keep-alive 502 · [ ] B34 타임아웃 계층 · [ ] B35 Actuator heapdump · [ ] B36 JWT alg=none · [ ] B37 압축폭탄 · [ ] B38 Log4Shell · [ ] B39 XXE · [ ] B40 SSRF IMDS · [ ] B41 역직렬화 · [ ] B42 xz
- [ ] B43 expand-contract · [ ] B44 설정배포 · [ ] B45 삭제 스크립트 · [ ] B46 left-pad · [ ] B47 Roblox · [ ] B48 카디널리티 · [ ] B49 protobuf

**I 트랙 (11)**
- [ ] I01 인증서 만료 · [ ] I02 CPU 스로틀링 · [ ] I03 conntrack · [ ] I04 포트/TIME_WAIT · [ ] I05 컨테이너 JVM · [ ] I06 헬스체크 · [ ] I07 롤링 유실 · [ ] I08 디스크 풀 · [ ] I09 DNS TTL · [ ] I10 IOPS 절벽 · [ ] I11 Toyota

**R 트랙 (18)**
- [ ] R01 승격 유실 · [ ] R02 DNS 캐시 · [ ] R03 리더 쏠림 · [ ] R04 복제슬롯 WAL · [ ] R05 스토리지 풀 · [ ] R06 커넥션 스톰 · [ ] R07 Proxy 피닝 · [ ] R08 pending-reboot · [ ] R09 gp2 절벽
- [ ] R10 binlog · [ ] R11 콜드 캐시 · [ ] R12 PI 클론 · [x] R13 슬롯 카운터 · [ ] R14 문자셋·타임존 · [ ] R15 RPO 스위치오버 · [ ] R16 배치 캐시오염 · [ ] R17 시계열 파티션 · [ ] R18 채팅 RDB

**S 트랙 (12)**
- [ ] S01 매핑 폭발 · [ ] S02 딥 페이지네이션 · [ ] S03 워터마크 · [ ] S04 bulk 리젝션 · [ ] S05 Nori · [ ] S06 서킷브레이커 · [ ] S07 오버샤딩 · [ ] S08 값비싼 쿼리 · [ ] S09 무중단 reindex · [ ] S10 refresh_interval · [ ] S11 머지 폭풍 · [ ] S12 GitHub 2013

**V 트랙 (8)**
- [ ] V01 필터 recall · [ ] V02 HNSW 스윕 · [ ] V03 연산자 불일치 · [ ] V04 메트릭·정규화 · [ ] V05 빌드 메모리 · [ ] V06 하이브리드 RRF · [ ] V07 메모리 초과 · [ ] V08 tombstone

**L 트랙 (10)**
- [ ] L01 무한 루프 · [ ] L02 인젝션 · [ ] L03 KV캐시 선점 · [ ] L04 컨텍스트 초과 · [ ] L05 청킹·recall · [ ] L06 스키마 강제 · [ ] L07 SSE 끊김 · [ ] L08 배칭 p99 · [ ] L09 양자화 · [ ] L10 Judge 편향

**M 트랙 (14)**
- [ ] M01 푸시 herd · [ ] M02 멱등성 · [ ] M03 토큰 레이스 · [ ] M04 인증서 피닝 · [ ] M05 IAP 웹훅 · [ ] M06 flush storm · [ ] M07 계약 파괴 · [ ] M08 강제업데이트 · [ ] M09 대량발송 · [ ] M10 좀비 토큰 · [ ] M11 좀비 소켓 · [ ] M12 Firebase 과금 · [ ] M13 App Links · [ ] M14 다중기기

**F 트랙 (21)**
- [ ] F01 한맥 · [ ] F02 삼성증권 · [ ] F03 9시 폭주 · [ ] F04 캐시vs원장 · [ ] F05 평가금액 · [ ] F06 ATS 라우팅 · [ ] F07 IPO 라이브락 · [ ] F08 flash freeze · [ ] F09 음수 가격 · [ ] F10 설정 드리프트 · [ ] F11 TSB
- [ ] F12 Monzo · [ ] F13 매칭엔진 · [ ] F14 정정 우선순위 · [ ] F15 슬로우 컨슈머 · [ ] F16 FIX 갭 · [ ] F17 BigDecimal · [ ] F18 영업일·DST · [ ] F19 cutoff · [ ] F20 규제 DR · [ ] F21 채번 병목

**X 트랙 (4)**
- [ ] X1 Stripe 헬스체크 괴리 · [ ] X2 Slack 게이트웨이 · [ ] X3 Cloudflare 부분 HA · [ ] X4 카카오 DC 편중

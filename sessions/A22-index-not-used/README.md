# A22 인덱스는 있는데 쿼리가 못 쓴다, 다섯 가지 조건

> 근거 등급: `E2`
> 출처: [MySQL 8.4, Type Conversion](https://dev.mysql.com/doc/refman/8.4/en/type-conversion.html) · [How MySQL Uses Indexes](https://dev.mysql.com/doc/refman/8.4/en/mysql-indexes.html) · [B-Tree Index Characteristics](https://dev.mysql.com/doc/refman/8.4/en/index-btree-hash.html) · [CREATE INDEX (functional key parts)](https://dev.mysql.com/doc/refman/8.4/en/create-index.html) · 실무 사례: [LINE, 함수형 인덱스로 비트 연산 쿼리 최적화](https://techblog.lycorp.co.jp/ko/solving-slow-queries-optimizing-bitwise-operation-queries-with-functional-indexes)

## 1. 유명한 이유

슬로우 쿼리를 받아 들고 처음 하는 일은 인덱스 확인입니다. 그런데 `SHOW INDEX`를 찍어 보면 인덱스가 이미 있습니다. 이때부터가 진짜 문제입니다. **인덱스가 없어서 느린 게 아니라, 있는 인덱스를 쿼리가 쓰지 못하게 작성돼 있어서 느린 경우**가 실무 슬로우 쿼리의 큰 몫을 차지합니다.

MySQL 공식 문서가 조건을 직접 적어 둡니다. 문자열 컬럼을 숫자와 비교하면 "인덱스를 써서 값을 빨리 찾을 수 없다"고 하고, utf8mb4 컬럼과 latin1 컬럼 비교는 "인덱스 사용을 배제한다"고 하며, LIKE는 "와일드카드로 시작하지 않는 상수 문자열일 때" 인덱스를 쓴다고 명시합니다.

실무 사례도 있습니다. LINE은 LINE VOOM 헤비 유저의 프로필 조회가 30초 타임아웃에 걸리는 문제를 공개했습니다. `WHERE user_id=? AND category_flag & 0x0100` 형태의 비트 연산 조건이 인덱스를 타지 못해 그 유저의 게시물 전체를 훑고 있었고, MySQL 8.0.13의 함수형 인덱스와 등가 비교 재작성으로 스캔을 805행에서 31행으로 줄였습니다.

이 세션은 그 조건들을 한자리에 모아 **인덱스는 그대로 두고 쿼리만 바꿔서** 전후를 잽니다. 인덱스를 추가해 빨라지는 것은 당연하지만, 같은 인덱스로 1,969배가 갈리는 것은 다른 이야기입니다.

## 2. 재현

### 환경

| 항목 | 값 |
|---|---|
| DB | MySQL 8.4.3 (컨테이너 `--cpus=4`, 버퍼 풀 2GB) |
| 데이터 | 주문 300만 행 (579MB), 레거시 조인 테이블 30만 행 |
| 측정 | 워밍업 3회 후 10회, 중앙값·p95. 실제로 읽은 행은 `Handler_read_*` 세션 카운터로 실측 |

버퍼 풀을 데이터보다 크게 잡았습니다. 캐시 미스가 섞이면 "인덱스를 못 탄 비용"과 "디스크 비용"이 뒤엉켜서, 무엇 때문에 느린지 구분되지 않습니다.

**읽은 행 수를 `EXPLAIN`의 `rows`로 세지 않았습니다.** 그 값은 옵티마이저의 추정이고, 실제로 몇 행을 만졌는지는 `Handler_read_next`와 `Handler_read_rnd_next`의 증가분이 답입니다. A09 세션에서 추정과 실제가 2,400배까지 벌어지는 것을 이미 봤기 때문에, 여기서는 처음부터 실측 카운터를 씁니다.

### 데이터 분포

균등 분포로 만들면 어떤 쿼리를 써도 비슷하게 나옵니다. 실제 주문 데이터를 닮게 만들었습니다.

- `order_no`: `ORD` + 12자리. 유니크에 가까움 (형변환 함정의 무대)
- `user_id`: Zipf(s=1.1) 쏠림. 1위 유저가 393,890건(13.1%)으로 LINE 사례의 "헤비 유저"에 해당
- `status_flag`: 비트 플래그. 선물 주문(0x0100) 90,543건(3.0%), 공개(0x0001) 60.0%
- `buyer_name`: 성 10종 × 이름 10종. `민준`으로 끝나는 이름 300,309건

## 3. 재현 결과

![인덱스 전후](results/chart-index.png)

| 조건 | 못 탈 때 | 탈 때 | 배수 | 읽은 행 (전 → 후) |
|---|---|---|---|---|
| 암묵적 형변환 | 829.6ms | **0.42ms** | 1,969배 | 3,000,001 → 0 |
| 문자셋 불일치 조인 | 1,436.0ms | 22.04ms | 65배 | 600,000 → 6,000 |
| 인덱스 컬럼에 함수 적용 | 294.6ms | **0.52ms** | 564배 | 3,000,000 → 0 |
| 선행 와일드카드 LIKE | 328.3ms | 67.16ms | 5배 | 3,000,000 → 300,309 |
| 비트 연산 조건 | 351.3ms | 20.05ms | 18배 | 393,890 → 11,922 |

![EXPLAIN 전후](results/fig-explain.png)

읽은 행이 0으로 나오는 두 케이스는 인덱스 탐색만으로 답을 찾아 스캔이 아예 없었다는 뜻입니다(`Handler_read_key`만 증가).

### 각 조건이 성립하는 이유

**암묵적 형변환.** `order_no`는 `VARCHAR(32)`인데 `WHERE order_no = 1500000`처럼 숫자와 비교하면, MySQL은 양쪽을 부동소수점으로 변환해 비교합니다. `'1'`, `' 1'`, `'1a'`가 전부 1로 변환되므로 인덱스에 정렬된 문자열 순서로는 시작점을 정할 수 없습니다. 애플리케이션이 주문번호를 `Long`으로 바인딩하면 이 상황이 됩니다.

**문자셋 불일치 조인.** `orders`(utf8mb4)와 `order_legacy`(latin1)를 `order_no`로 조인하면 한쪽을 변환해야 하고, 변환된 값으로는 인덱스를 탈 수 없습니다. 실행계획을 보면 옵티마이저가 아예 레거시 테이블을 선두로 놓고 30만 행을 훑는 쪽으로 방향을 틉니다.

**함수 적용.** `WHERE DATE(created_at) = '2026-03-15'`는 인덱스에 저장된 `created_at` 값과 비교 대상이 다릅니다. 범위 조건 `>= '2026-03-15' AND < '2026-03-16'`으로 펴면 같은 의미인데 인덱스를 탑니다.

**선행 와일드카드.** `LIKE '%민준'`은 앞이 열려 있어 인덱스 시작점을 못 정합니다. 여기서 `LIKE '김%'`로 바꾸는 건 **다른 질문**이라 비교가 성립하지 않습니다. 같은 질문("이름이 민준으로 끝나는")을 인덱스로 답하려면 뒤집어 저장해야 합니다. 생성 컬럼 `buyer_name_rev = REVERSE(buyer_name)`에 인덱스를 걸고 `WHERE buyer_name_rev LIKE CONCAT(REVERSE('민준'),'%')`로 조회하면 앞이 고정됩니다. 양쪽 결과가 300,309건으로 같은 것을 확인했습니다.

**비트 연산 (LINE 사례).** `WHERE user_id = 1 AND (status_flag & 0x0100)`은 계산 결과를 조건에 쓰므로 일반 인덱스로 못 찾습니다. 함수형 인덱스 `(user_id, ((status_flag & 0x0100)))`를 만들고 **등가 비교** `(status_flag & 0x0100) = 256`으로 바꿔야 탑니다. 표현식이 인덱스 정의와 정확히 일치해야 한다는 조건이 여기서 걸립니다.

## 4. 해소

| 조건 | 해소 | 실무에서의 형태 |
|---|---|---|
| 형변환 | 바인딩 타입을 컬럼 타입에 맞춤 | ORM 파라미터 타입 점검, 가능하면 컬럼을 숫자 타입으로 |
| 문자셋 | 조인 양쪽 컬럼의 문자셋·콜레이션 통일 | 마이그레이션을 끝까지. 임시로는 `COLLATE` 명시도 인덱스는 못 살림 |
| 함수 | 컬럼을 감싸지 말고 상수 쪽을 가공 | 날짜는 범위 조건, 문자열은 상수를 변환 |
| 선행 와일드카드 | 역순 생성 컬럼 + 인덱스, 또는 전문 검색 | 끝말 검색이 정말 필요한지부터 확인 |
| 비트 연산 | 함수형 인덱스 + 표현식 일치 등가 비교 | MySQL 8.0.13 이상. 표현식을 코드 상수로 관리 |

## 5. 예상과 달랐던 점

### 선행 와일드카드만 개선 폭이 작았습니다

다섯 중 넷은 두 자릿수에서 네 자릿수 배수인데 LIKE만 5배입니다. 역순 컬럼으로 인덱스를 타긴 하지만 결과가 300,309건이라 그만큼은 어차피 읽어야 하기 때문입니다. **인덱스는 읽을 행을 찾는 비용을 줄이지, 읽어야 할 행 자체를 줄이지 않습니다.** 결과 집합이 큰 쿼리는 인덱스로 해결되지 않고 요구사항을 다시 봐야 합니다.

### 함수형 인덱스 문법에서 한 번 막혔습니다

`ADD KEY idx_gift ((user_id), ((status_flag & 0x0100)))`으로 만들려다 `ERROR 3762 Functional index on a column is not supported`를 받았습니다. 일반 컬럼을 괄호로 감싸면 함수형 키 파트로 해석되는데, 컬럼 그 자체는 표현식이 아니라서 거부됩니다. `(user_id, ((status_flag & 0x0100)))`처럼 표현식 부분만 이중 괄호를 씌워야 합니다.

### 실험 도구가 또 한글에 걸렸습니다

역순 컬럼을 검증하려고 `docker exec`로 `LIKE '%민준'`을 날렸더니 0건이 나왔습니다. R14에서 다뤘던 접속 문자셋 문제를 그대로 다시 밟은 것으로, `--default-character-set=utf8mb4`를 주자 300,309건이 나왔습니다. 벤치마크는 PyMySQL(utf8mb4)로 돌아서 정상이었고 확인용 CLI만 깨져 있었습니다. 같은 함정을 아는 상태에서도 도구를 바꾸면 다시 밟습니다.

### `REVERSE()`는 바이트가 아니라 문자 단위였습니다

멀티바이트에서 뒤집으면 글자가 깨질 것으로 걱정했는데, `최도윤`이 `윤도최`로 정확히 뒤집혔습니다(HEX로 확인). 한글에서도 역순 인덱스 기법을 쓸 수 있다는 뜻입니다.

## 못 한 것

- **OR 조건과 index merge는 다루지 않았습니다.** 다섯 가지로 좁혔고, OR은 옵티마이저 버전에 따라 동작이 갈려 별도 세션이 낫습니다.
- **결과 집합이 큰 쿼리의 해법을 제시하지 않았습니다.** LIKE 케이스에서 드러난 한계인데, 커버링 인덱스나 요구사항 재정의가 필요한 영역입니다.
- **PostgreSQL 대조가 없습니다.** 형변환 규칙과 함수 인덱스 지원이 달라서 같은 실험이 다르게 나옵니다.
- **LINE 사례의 원 수치(805행 → 31행)와 직접 비교할 수 없습니다.** 데이터도 스키마도 다른 축소 재현이고, 메커니즘만 같습니다.

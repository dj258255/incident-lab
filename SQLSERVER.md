# SQL Server 세션 한 장 요약

이 저장소의 39세션 중 **SQL Server 를 직접 만진 아홉**을 한 줄씩 추렸습니다.
각 줄은 **무엇을 물었나 / 실측 하나 / 그래서 운영에서 정한 것** 순서입니다.
자세한 것은 각 세션의 README 와 `results/` 원문에 있습니다.

환경은 전부 **SQL Server 2022 Developer · 로컬 Docker**입니다.
ARM 맥 에뮬레이션이라 **실행 시간은 근거로 쓰지 않고**, 락 개수·행 수·논리 읽기·오류 번호·상태 전이처럼
하드웨어와 무관한 값만 씁니다.

---

## 동시성과 락

**[A04 락 승격](sessions/A04-mssql-lock-escalation/)** ([절차서](sessions/A04-mssql-lock-escalation/RUNBOOK.md))
문서가 말하는 임계 5,000 은 어디서 발동하는가.
→ 이분 탐색으로 찾은 경계는 **6,232행**(그때 락 6,249개). 확장 이벤트 두 필드를 갈라 보니
**검사는 문장 전체 락으로 1,250개마다, 승격은 참조 하나의 락이 5,000을 넘을 때**.
갱신 컬럼에 보조 인덱스가 하나 붙으면 행당 락이 2개가 되어 경계가 2,908행으로 내려간다.
그리고 **승격은 트랜잭션이 아니라 문장 단위** — 한 트랜잭션에서 300행씩 60문장으로 1만 8천 락을
쌓아도 안 나고, 같은 수를 한 문장으로 잡으면 난다. **이것이 배치 분할이 통하는 진짜 이유다.**
→ 배치 상한을 5,000 미만으로 강제하되, **인덱스 수를 세고 정한다.**

**[A27 조인 연산자](sessions/A27-mssql-physical-join/)**
옵티마이저는 Nested Loops·Merge·Hash 중 무엇을 왜 고르는가.
→ 논리 읽기가 셋 다 3,119 로 같은데 **`OPTION (MERGE JOIN)` 은 메모리를 69,840KB 요구해 Hash 의 17배**.
힌트를 걸 때 읽기만 보면 이 차이가 안 보인다. 통계를 낡게 만들어 **추정이 408배 어긋나게 해도
연산자는 안 뒤집혔다** — "통계가 낡으면 계획이 뒤집힌다"가 이 규모에서는 안 일어났다.
→ 힌트 판정은 읽기가 아니라 **메모리 요구까지** 본다.

---

## 데이터 보정 (공고 담당업무 직결)

**[A24 이상 탐지](sessions/A24-currency-anomaly-detection/)** ([절차서](sessions/A24-currency-anomaly-detection/RUNBOOK.md))
"재화가 이상하다"는 보고에서 회수 대상을 어떻게 확정하는가.
→ 원장 2,000만 행에서 세 방식을 정답지와 대조. **짝 맞추기·한도는 오탐 0, 통계 이탈은
어뷰저 60개를 다 잡으면서 정상 헤비 이용자 40명을 함께 지목**. 쏠린 분포에서는 5시그마까지
올려도 지목의 53.8%가 무고하다.
→ **통계로는 후보까지, 확정은 결정론으로 판정되는 것만.**

**[A25 회수 프로시저](sessions/A25-currency-reclaim-procedure/)** ([절차서](sessions/A25-currency-reclaim-procedure/RUNBOOK.md))
이미 써 버린 재화를 서비스를 안 멈추고 어떻게 되돌리는가.
→ 계정 100만·대상 6만(2만은 잔액 부족)에서 저장 프로시저를 짰다. **4,000행 배치 커밋으로 승격 0회**,
다섯째 배치에서 일부러 죽여도 **이중 회수 0·누락 0**(진행 지점을 회수와 같은 트랜잭션에서 옮겼기 때문).
데드락은 **엔진에 맡기면 9회 모두 게임 트래픽 쪽이 희생** — 엔진의 기준은 되돌리는 비용이지
서비스 영향이 아니다. `DEADLOCK_PRIORITY LOW` 를 보정에 주자 3/0 으로 뒤집혔다.
→ 재시도는 **트랜잭션 밖**에, **1205 만** 잡는다(나머지를 삼키면 사고를 재시도로 덮는다).
감사 표에 계정별 **전후 잔액**을 남겨 이의 제기의 근거로 삼고, 끝에 **대상액 = 적용액 + 미회수액**을 검산한다.

---

## 백업·복구·가용성

**[A23 백업과 시점 복구](sessions/A23-backup-pitr/)**
백업이 있는데 복구가 안 되는 자리는 어디인가. (MySQL·PostgreSQL·SQL Server)
→ **복원을 에러 없이 통과하고도 데이터가 없는 백업이 넷 중 둘.** 백업만으로는 500건이
유실되는 사고를 시점 복구로 0건까지. 그리고 **DB 가 복구돼도 앱이 20.9초 더 죽어 있었다**(JVM DNS 캐시).
→ 성공 판정은 로그가 아니라 **실복원 + 기준 데이터 대조**. RTO 는 DB 가 열린 시점이 아니라
**앱이 읽기·쓰기를 둘 다 정상 처리한 시점**으로 잰다.

**[A28 페이지 손상과 페이지 복원](sessions/A28-mssql-corruption-page-restore/)**
지금 돌고 있는 DB 가 조용히 손상됐다면 무엇이 알려 주고 어디까지 되돌릴 수 있는가.
→ `.mdf` 페이지 한 장(1:450)에서 32바이트를 0으로 덮고 ONLINE 으로 되돌려도 **아무 오류가 없다**.
읽는 순간 **824**(`incorrect checksum`)로 죽고 `msdb.dbo.suspect_pages` 에 **event_type 2** 로 자동 기록.
**페이지만 복원하고 꼬리 로그까지 재생하니 백업 이후에 쓴 10행까지 살아 손실 0**(suspect_pages 2 → 4 전이).
같은 손상에 `REPAIR_ALLOW_DATA_LOSS` 를 쓰면 **30,000행이 29,923행 — 77행이 사라지고 어디에도 안 남는다.**
→ **발견 → CHECKDB 로 범위 확정 → 체인이 있으면 페이지 복원 → 없을 때만 repair.**
두 길의 차이는 로그 체인 하나다.

**[A29 로그 전달과 페일오버](sessions/A29-mssql-log-shipping-failover/)**
승격하면 무엇을 잃는가. (A23 의 MySQL 반동기는 **커밋 334건**을 잃었다)
→ 두 라운드 전달에서 보조가 10,005 → 10,010 을 따라오고, **아직 백업 안 된 3행이 보조에 없다 —
그 차이가 RPO**. 꼬리 로그를 `WITH NORECOVERY` 로 뜨면 **주가 RESTORING 으로 내려가 쓰기를 거부**하고,
보조가 꼬리까지 복원해 열면 **10,013행으로 장애 직전 주와 정확히 같다(유실 0)**.
그리고 **승격이 끝나도 앱은 못 들어온다** — 로그인은 master 에 남아, 같은 이름으로 만들어도
**SID 가 달라 고아**(user `0x6C0C…` vs login `0x4765…`).
→ 유실을 정하는 것은 기종이 아니라 **절차**다. 페일오버 절차서의 마지막 줄은 DB 가 아니라 **로그인**이다
(`ALTER USER ... WITH LOGIN` 이 수리, 예방은 이관 때 `WITH SID`).

---

## 진단

**[A26 진단 순서](sessions/A26-mssql-diagnosis/)** ([절차서](sessions/A26-mssql-diagnosis/RUNBOOK.md))
"느려졌다"는 신고 한 줄에서 원인까지 어떤 순서로 좁히는가.
→ **대기 통계는 누적**이라 같은 `LCK_M_IS` 가 누적 430,521ms 대 이번 사고 107,453ms 로 네 배 차이 —
두 번 떠서 차분으로 봐야 한다. 블로킹은 `blocking_session_id` 를 그대로 보면 중간 세션이 나와
재귀로 사슬(53←52←51)을 따라가야 하고, **그 뿌리는 sleeping 이라 `dm_exec_requests` 에 안 나온다**
(요청은 없는데 트랜잭션만 열린 세션). 계획 캐시는 리터럴이 흩어져 60개 계획으로 숨고,
`query_hash` 로 묶어야 실제 부하가 151,326 → 304,086 으로 드러난다.
→ **대기 → 막은 뿌리 → 쿼리** 순서. 각 단계의 함정을 절차서에 적어 뒀다.

**[A22 인덱스를 못 쓰는 조건](sessions/A22-index-not-used/)** · **[A09 통계와 계획 뒤집힘](sessions/A09-planner-stats-flip/)**
인덱스가 있는데 왜 안 빨라지는가.
→ 조건 컬럼이 빠진 **640MB 인덱스는 논리 읽기 229,614 를 228,399 로** 거의 못 줄인다.
그 컬럼을 넣으면 **496**, 필터드 인덱스는 크기 절반에 **482**. 다만 **필터드는 조회 쪽 SET 옵션이
하나만 안 맞아도 에러 없이 무시**되어 229,614 로 돌아간다.
→ 인덱스는 **크기가 아니라 조회 모양**이 정한다. 그리고 만드는 것으로 끝이 아니라 세션 환경까지 확인한다.

---

## 이 랩의 규율

- **재 보지 않은 것을 근거로 쓰지 않는다.** ARM 에뮬레이션이라 시간은 인용하지 않는다
- **틀린 결론은 지우지 않고 정정 기록을 남긴다.** 락 승격 경계가 계속 내려간다고 적었다가
  다음 실험에서 반증돼 고친 것이 그 예다
- **예상과 다른 결과도 그대로 적는다.** A27 에서 통계를 408배 어긋나게 해도 연산자가 안 뒤집힌 것,
  A24 에서 파티셔닝이 조사를 안 빠르게 한 것(638 대 657)

# 작성 규약

세션 하나를 쓰고 발행하기까지 지키는 규칙입니다. 새 세션을 시작할 때 1~5장을,
발행 직전에 11장(자기 검증)을 다시 읽습니다.

| | | |
|---|---|---|
| [1. 디렉토리](#1-디렉토리) | [5. 트랙 구분 기준](#5-트랙-구분-기준-a와-r) | [9. 보안 세션](#9-보안-세션) |
| [2. README 구성](#2-readme-구성) | [6. 수치](#6-수치) | [10. 커밋](#10-커밋) |
| [3. 근거 등급](#3-근거-등급) | [7. 문체](#7-문체) | [11. 발행 전 자기 검증](#11-발행-전-자기-검증-필수) |
| [4. 구현 언어](#4-구현-언어) | [8. 재현 기록](#8-재현-기록reproducemd) | [12. 재현 계층](#12-재현-계층-최소-재현과-실제-스택) |


## 1. 디렉토리

```
sessions/A02-mdl-storm/
├── README.md        회고 본문
├── RUNBOOK.md       운영 절차서 (사고 대응 세션에만. 시간 구간별 단계와 판정 기준)
├── reproduce.md     실행 명령과 출력 원문
├── compose.yml      재현 환경
├── scripts/         부하·주입·측정 스크립트
└── results/         측정 데이터(csv·png), 렌더 스펙(render.json), 실행 화면(png)
```

저장소 루트의 `tools/render/`는 실행 화면과 수치를 PNG로 만드는 공용 렌더러입니다.

기존 컨테이너를 재사용하는 세션은 `compose.yml` 대신 README 의 실험 절에 어느 인스턴스를
왜 재사용했는지 적습니다(메모리가 모자라 새로 못 띄우는 경우). `reproduce.md` 도 실행이
`scripts/exp*.sh` 한 줄로 끝나고 출력 원문이 `results/*.txt` 에 그대로 남는 세션은 생략합니다.

이름은 `<트랙><번호>-<영문-슬러그>` 입니다. 번호는 [CATALOG.md](CATALOG.md)를 따릅니다.

## 2. README 구성

**첫 화면에 결론이 보여야 합니다.** 읽는 사람은 이 세션이 무엇을 밝혔는지 알아야
계속 읽을지 정합니다. 스크롤해야 결론이 나오면 대부분 거기서 닫습니다.

### 표준 구성

| 절 | 내용 | 필수 |
|---|---|---|
| **제목 아래 한 줄** | 주제 / 버전 / 근거 등급 (`E1`·`E2`·`E1·축소`) | 필수 |
| **이 세션이 필요한 상황** | 운영에서 언제 이 지식이 필요한가. 2~3줄 인용구 | 권장 |
| **결론부터** | 밝힌 것을 표로. 각 행은 `주장 + 근거 수치 + 본문 앵커 링크` | 필수 |
| **재현** | 환경 표(호스트·컨테이너·데이터 규모), 그리고 **무엇을 근거로 쓰지 않는지** | 필수 |
| **본문 1..n** | 실험 하나에 절 하나. 명령과 출력을 그대로 | 필수 |
| **표준 처방** | 그래서 운영에서 무엇을 정하는가 | 필수 |
| **예상과 달랐던 점** | 없으면 그 세션은 아직 덜 판 것 | 필수 |
| **못 한 것** | 한계를 먼저 적는 쪽이 신뢰를 만든다 | 필수 |

`결론부터` 표의 각 행은 **본문 앵커로 링크합니다.** 주장을 의심하는 사람이 바로
증거로 점프할 수 있어야 하고, 이 링크가 없으면 표는 그냥 자기 주장입니다.

### 구형 6절 구성 (전환 중)

초기 세션은 아래 6절로 썼습니다. 손볼 일이 생길 때 위 표준 구성으로 옮깁니다.
`결론부터`와 `못 한 것`이 없는 세션이 여기 해당합니다.

> 1. 유명한 이유 → 2. 재현 → 3. 내부 원리 → 4. 해소 → 5. 재계측 → 6. 예상과 달랐던 점

절 제목에 **날짜를 넣지 않습니다.** `## 가드에 구멍이 있을 때 (2026-07-31)` 처럼
증보 시점을 제목에 박으면 문서가 작업 일지처럼 읽힙니다. 언제 더했는지는 커밋이 압니다.

### 절 번호는 한번 붙으면 재배치하지 않습니다

번호가 어긋나 보여도 다시 매기지 않습니다. **제목이 곧 앵커**라서, 번호를 하나 밀면
`결론부터` 표의 링크와 본문의 "N절" 참조가 함께 죽습니다. 에러가 안 나므로
눌러 보기 전까지 아무도 모릅니다. 발행한 글이나 남의 북마크가 그 앵커를 걸고 있으면
저장소 밖에서도 깨집니다.

실제로 한 번 밟았습니다. 전 세션의 번호를 정렬하는 스크립트를 돌렸더니 A27 의
무번호 `재현` 에 번호가 붙었고, 그 바람에 본문 1~6 이 2~7 로 밀려 그 세션의
`결론부터` 표 앵커가 통째로 깨졌습니다. 되돌리고 날짜 제거만 남겼습니다.

- 증보 절은 **뒤에 이어 붙입니다.** 번호가 없으면 없는 대로 둡니다.
- 앞쪽에 절을 끼워야 하면 **하위 번호**를 씁니다(`5-2.`, `6-1.`). 뒤가 안 밀립니다.
- 번호가 정말 중복이면 그 절만 하위 번호로 내리고, **본문의 "N절" 참조를 같이 옮깁니다.**
- 고치고 나면 `python3 tools/check-anchors.py` 로 확인합니다.

## 3. 근거 등급

| 등급 | 조건 | 본문에서 쓰는 표현 |
|---|---|---|
| `E1` | 기업 포스트모템·규제기관 문서 원문 존재 | "○○사가 공개한 장애" |
| `E2` | 벤더 공식 문서·CVE·업계 통계가 경고 | "실무에서 반복되는 함정" |
| `E1·축소` | 사건은 실재하나 그대로 재현 불가 | "사건 자체는 재현할 수 없어 같은 메커니즘을 축소 재현했다" |

**등급을 올려 쓰지 않습니다.** 특정 회사 사건이 아닌 것을 "○○사에서 터진 문제"라고 쓰면 안 됩니다. 축소 재현을 "사건을 재현했다"고 쓰면 안 됩니다.

출처를 확인하지 못한 항목은 **쓰지 않습니다.** 확인 시도와 실패 기록은 CATALOG의 검증 로그에 남깁니다.

## 4. 구현 언어

- **재현 대상이 애플리케이션 계층이면 Java와 Spring Boot로 만듭니다.** ORM 동작, 트랜잭션 경계, 커넥션 풀처럼 애플리케이션 코드가 원인이거나 해소 지점인 세션이 여기 해당합니다.
- 부하 생성기(k6), 계측·수집·집계 스크립트(Python), 순수 SQL 재현은 도구이므로 이 규칙의 대상이 아닙니다.
- 문제 자체가 특정 언어 생태계의 것이면(예: PG 드라이버 동작) 그 언어를 따릅니다.

## 5. 트랙 구분 기준 (A와 R)

겹쳐 보이는 두 트랙의 기준은 "관리형 환경이라는 조건이 서사에 필수인가"입니다.

- **A**: DB 엔진 자체의 동작이 주제. 어디에 설치돼 있든 같은 결과.
- **R**: 호스트 접근 불가, 파라미터 그룹, IOPS 과금, 승격 절차, 관리형 관측 도구처럼 **RDS·Aurora라는 조건이 문제나 해소를 바꾸는** 세션. 본문에 그 관점이 들어가야 하고, 결론에 클라우드 항목을 한 절 이상 둡니다.

## 6. 수치

- 측정 조건을 함께 적습니다. `2,832 req/s`가 아니라 `10 VU 30초에서 2,832 req/s, P95 5.86ms`
- 평균만 쓰지 않습니다. 평균과 P95를 함께 적습니다
- 개선 전을 먼저 재고, 고친 뒤 **같은 조건에서** 다시 잽니다
- 재현할 수 없는 수치는 인용하지 않습니다

### 수치의 시각 증거 (results/ 이미지)

수치는 본문 서술과 reproduce.md 원문으로도 검증되지만, 재현 화면을 이미지로 남기면 신뢰가 더 붙습니다. 이미지는 손으로 만들지 않습니다. `results/render.json`에 캡처한 콘솔 출력과 측정값을 적고 렌더러가 생성하므로, 리뷰어가 다시 돌려 같은 그림을 얻을 수 있습니다.

- 재현(2절)에 문제가 드러난 실행 화면 한 장, 재계측(5절)에 해소된 실행 화면과 전후 막대그래프를 넣습니다. 절마다 한두 장이면 충분합니다.
- 콘솔 카드는 reproduce.md의 출력과 글자까지 일치해야 합니다. 없는 줄을 지어내지 않습니다.
- 강조색은 의미로만 씁니다. 막아낸 지점은 초록, 문제가 드러난 수치는 빨강, 안전장치 발동은 노랑.
- 생성은 `docker build -t incident-lab-render tools/render` 뒤 `docker run --rm -u "$(id -u):$(id -g)" -v "$PWD":/work incident-lab-render sessions/<세션>/results`. 사용법은 [tools/render/README.md](tools/render/README.md)에 있습니다.

## 7. 문체

- 합니다체. 이모지 없음. 문장 안 대시(—) 없음
- 사건 서술은 출처를 먼저 밝힙니다. 유명한 문제임을 인정하는 톤이 신뢰를 만듭니다
- 기능 나열 대신 "어떤 상황에서 무엇이 깨졌고 그래서 무엇을 바꿨나"의 서사로 씁니다
- 안 한 것과 못 한 것을 적습니다. 한계를 먼저 적는 쪽이 신뢰를 만듭니다

## 8. 재현 기록(reproduce.md)

실행한 명령과 출력을 **원문 그대로** 붙입니다. 요약하거나 다듬지 않습니다.

````markdown
## 1. 환경 기동

```console
$ docker compose up -d
[+] Running 3/3
 ✔ Container lab-mysql   Started
```

## 2. 증상 재현

```console
$ mysql -e "SHOW ENGINE INNODB STATUS\G" | grep -A5 "LATEST DETECTED DEADLOCK"
...실제 출력...
```
````

## 9. 보안 세션

- 격리된 로컬 네트워크에서만 실행합니다
- 외부 호스트를 대상으로 하지 않습니다
- 동작하는 익스플로잇 코드를 저장소에 올리지 않습니다. 방어 검증에 필요한 최소 재현까지만 다룹니다
- 공급망 공격(xz 등)은 실제 백도어를 재현하지 않고 원리만 무해한 예제로 설명합니다

## 10. 커밋

- 한국어 완성 문장. 이모지 없음
- 세션 하나가 끝나면 한 커밋으로 정리합니다: `A02 온라인 DDL 메타데이터 락 폭풍 재현과 해소`
- 진행 중 저장은 `wip:` 접두어를 붙입니다

### 세션을 끝낼 때 같이 고치는 파일

세션 디렉토리만 만들고 끝내면 목록이 실제와 어긋납니다. 실제로 2026-07-29에 완료 18개 중
5개가 README 목록에서 빠져 있었고 개수도 12로 멈춰 있었습니다. 커밋 전에 셋을 함께 고칩니다.

1. `CATALOG.md`의 해당 행에 **완료** 표기와 세션 링크를 넣고, 하단 진행 체크리스트의 `[ ]`를 `[x]`로 바꿉니다
2. `README.md`의 진행 현황 표에서 완료와 대기 수를 고칩니다
3. `README.md`의 완료 목록에 한 줄을 추가합니다. 사건 배경이 아니라 그 세션이 실측한 수치를 담습니다

## 11. 발행 전 자기 검증 (필수)

2026-07-29에 완료 세션 22개를 원본 파일과 1차 문서로 전수 대조했습니다. 수치를 지어낸 곳은
없었지만, **측정한 것과 결론이 어긋나는 곳**이 여러 편에서 나왔습니다. 아래는 그때 실제로
걸린 것들이고, 같은 함정이 새 세션에서 다시 나왔습니다. 발행 전에 하나씩 확인합니다.

### 수치

- [ ] **같은 수치가 세 곳에서 같은가.** `results/`의 실행 로그, `reproduce.md`, `README.md`.
      A23은 복구 시간이 세 곳에서 `.075473 / .077 / 0.077`로 전부 달랐습니다.
- [ ] **그림 안의 수치가 본문과 같은가.** PNG에는 값이 박혀 있어 본문만 고치면 그림이 본문을
      반박합니다. B52는 차트가 4,896ms인데 바로 아래 표가 4,620ms였습니다. 서로 다른 실행분입니다.
- [ ] **배수를 주장하는 두 쿼리가 같은 결과 집합을 돌려주는가.** A22의 헤드라인 1,969배는
      양쪽이 모두 0건을 돌려주는 비교였습니다. 결과 건수를 결과 파일에 반드시 남깁니다.
- [ ] **비교하는 두 조건이 정말 한 변수만 다른가.** A18의 1.4배는 갱신 이력이 두 배 다른 두
      테이블 비교였고, F13의 해소판은 큐 구현·원자 구간·소비 방식 셋을 한꺼번에 바꿨습니다.
- [ ] **두 실행이 같은 부하를 받았는가.** F03은 버그 경로에 요청 6,598건, 해소 경로에 9,959건이
      도달했습니다. `dropped_iterations`를 확인하지 않으면 열린 모델이 깨진 것을 놓칩니다.
- [ ] **반복 측정을 했는가. 범위를 적었는가.** 한 번 재고 여덟 자리 유효숫자를 쓰지 않습니다.
      B43은 관측 범위가 11.3~28.0초인데 최솟값만 헤드라인에 썼습니다.

### 조건

- [ ] **호스트 사양을 찍어 남겼는가.** `uname -srm`, `nproc`, `free -g`. 이 저장소는 장비가 둘
      이상이라 세션 간 절대 시간 비교가 성립하지 않습니다. 기록이 없으면 추정하지 말고
      "기록하지 않았습니다"로 적습니다.
- [ ] **컨테이너 자원 한도를 적었는가.** `cpus`, `mem_limit`, JVM이면 `-Xmx`.
- [ ] **시간 수치가 있는데 호스트가 없지는 않은가.** 이 조합이 가장 자주 지적받습니다.
- [ ] **의도적으로 조인 값을 밝혔는가.** F15의 `SO_RCVBUF=8192`, B31의 `MaxMetaspaceSize=24m`
      처럼 결과를 만든 설정은 재현 조건입니다.

### 서술

- [ ] **근거 등급이 본문 주장과 맞는가.** 본문이 "축소 재현했습니다"라고 말하면 E1이 아니라
      E1·축소입니다. 원문을 직접 확인하지 못했으면 재인용임을 밝힙니다.
- [ ] **성과를 옳은 것에 돌렸는가.** F01의 "비정상 접수 0건"은 유한성 가드가 아니라 킬스위치가
      만든 결과였고, 가드는 그 실행에서 한 번도 호출되지 않았습니다.
- [ ] **결론이 자기 데이터와 어긋나지 않는가.** A18은 "인덱스 수에 비례한다"고 적어 놓고
      실측은 0개 267MB, 10개 595MB로 2.2배였습니다.
- [ ] **부재를 증거로 쓰지 않았는가.** B43의 "0 ms"는 1초 미만을 기록하지 않는 로그가 비어
      있다는 뜻이었습니다.
- [ ] **인용한 문서를 실제로 열어봤는가.** "문서에서 찾지 못했다"고 적은 것이 문서에 있던
      경우가 네 건이었습니다. MySQL 8.4는 8.0과 기본값이 여럿 다릅니다.
- [ ] **콘솔 블록이 진짜 출력인가.** 손으로 줄이거나 고치지 않습니다. A23은 `grep '^mysql'`
      결과를 4줄로 적었는데 실제 출력은 8줄이었습니다.

### 파일

- [ ] **결과 파일이 최신인가.** F15는 `report.txt`에 철회한 값이 남아 저장소가 글을 반박했습니다.
- [ ] **스크립트 주석과 라벨이 실제 실행과 같은가.** A08의 차트 각주는 6스레드였고 실제는
      프로세스 2개였습니다. 라벨은 손으로 쓰지 말고 결과 파일에서 읽어 씁니다.
- [ ] **적은 대로 재현되는가.** `docker compose up`부터 순서대로 따라가 봅니다. A18은 헤드라인
      두 조건이 실행 스크립트에 아예 없었고, B52는 필요한 테이블이 스키마에 없었습니다.
- [ ] **도구가 조용히 실패하지 않는가.** R13의 `termshot.py`는 크롬이 없으면 오류 없이 파일만
      만들지 않았습니다. 외부 명령은 `check=True`로 크게 실패시킵니다.
- [ ] **링크가 가 닿는가.** `python3 tools/check-anchors.py` 를 돌립니다. `결론부터` 표는
      행마다 본문 절을 앵커로 가리키므로 제목을 손대면 조용히 죽습니다. 블로그 절대 경로
      (`/blog/...`)를 그대로 쓰면 저장소에서 404 가 되므로 전체 URL 로 적습니다.

### 트랙별

- [ ] **R트랙이면 클라우드 절이 있는가.** §5가 요구합니다. RDS·Aurora에서 무엇이 달라지는지
      한 절 이상.
- [ ] **R트랙이면 비용을 한 문단 이상 다뤘는가.** 인스턴스 등급, I/O 과금, 회수 불가 스토리지.
      금액을 모르면 구조만 적되 "재지 않았습니다"로 넘기지 않습니다.

### 도구

- **`seq 1 $n` 을 n 이 0 일 수 있는 자리에 쓰지 않는다.** macOS 의 BSD `seq` 는
  `seq 1 0` 에 빈 출력이 아니라 `1` 과 `0` **두 줄**을 낸다. GNU `seq` 는 빈 출력이다.
  그래서 "0개 조건" 이 조용히 2개가 된다. A14 의 서브트랜잭션 실험에서 세이브포인트
  0개 조건에 세이브포인트가 두 개 붙어 트랜잭션당 XID 가 1 이 아니라 3 으로 나왔다.
  A18 은 같은 함정을 이미 알고 `if [ "$n" -gt 0 ]` 가드를 두고 있었는데 새 스크립트가
  그대로 다시 밟았다. **0 이 될 수 있으면 항상 가드를 둔다.**

  덧붙여 그 실험의 행 수 확인이 `-lt` 였다. 3배가 나와도 통과한다. **기대값이 정확히
  정해지는 자리에서는 `-ne` 로 본다.**

- **셸 변수 뒤에 한글을 바로 붙이지 않는다.** `"$N개"`는 bash 가 `N개`를 변수명으로 읽는다.
  UTF-8 로케일에서 `isalpha()`가 한글 바이트를 이름 문자로 받기 때문이다. `set -u`가 걸려
  있으면 `N개: unbound variable`로 스크립트가 죽고, 없으면 조용히 깨진 문자가 찍힌다.
  **항상 `"${N}개"`로 감싼다.** 2026-07-30 전수 조사에서 A09 와 B01 에서 실제로 깨지는
  세 곳을 찾아 고쳤다. 점검 명령은 이것이다.

  ```
  find sessions tools -name "*.sh" -print0 | xargs -0 grep -nE '\$[A-Za-z_][A-Za-z0-9_]*[가-힣]'
  ```

- **`docker exec` 에 `-i` 를 빠뜨리면 표준입력이 전달되지 않는다.** 에러도 나지 않는다.
  히어독으로 스크립트를 컨테이너에 심을 때 파일이 빈 상태로 남고, 그 뒤 측정 전체가
  무의미해진다. 심은 뒤 줄 수를 확인해 fail-closed 로 만든다.

- **DB 컨테이너 안에서 `pkill` 을 쓰지 않는다.** A01 에서 부하 루프를 `pkill -f w.sh` 로
  끄자 postgres 백엔드가 SIGTERM 을 받고 postmaster 가 전체를 정리하며 크래시 복구에
  들어갔다. 어느 프로세스가 매칭됐는지는 짚지 못했다(psql 을 초당 수십 개 포크하는 동안
  PID 재사용과 pkill 의 조회·전송 사이 경합이 유력하다). 원인을 단정하지 않고 **정지 파일을
  두고 루프가 스스로 빠지게** 바꿨다. 측정 전후로 `pg_postmaster_start_time()` 을 비교해
  재기동을 감지하는 가드도 함께 둔다.

- **초 단위 타임스탬프로 초 단위 사건을 재지 않는다.** A01 에서 2초짜리 ALTER 구간의
  쓰기를 `date +%s` 로 센 결과 창 밖의 쓰기가 섞여 "전환 중에도 초당 20건이 통과했다"는
  틀린 값이 나왔다. 밀리초(`date +%s%N`)로 바꾸니 3건이었다.

- **비교할 두 경로는 루프 구조부터 맞춘다.** A01 의 PostgreSQL 백필을 `WHERE id_new IS NULL`
  로만 돌자 부하가 방금 넣은 몇 행씩을 계속 쫓아 수렴하지 않았다(벌크 151회 뒤 28~30행짜리
  청크가 1400회 넘게 이어졌다). MySQL 쪽은 워터마크로 묶여 151회에서 끝났으므로 두 값을
  나란히 놓으면 틀린 비교가 된다. 시작 시점의 최대 id 를 워터마크로 잡아 구조를 맞췄다.

- **"만들었다"는 반환값을 안 보면 앞 조건이 다음 조건으로 새어 나온다.** R04 에서 조건마다
  복제 슬롯을 지우고 새로 만들었는데, `docker rm -f` 로 컨슈머를 죽여도 PostgreSQL 이
  walsender 의 죽음을 알아채기까지 잠깐 걸린다. 그 사이 `pg_drop_replication_slot` 은
  "슬롯이 활성 상태"라며 실패하고, 반환값을 안 보면 옛 슬롯이 그대로 남는다.
  그다음 조건이 앞 조건의 `restart_lsn` 을 물려받아 시작 지연이 300MB 로 찍혔다.
  **성공할 때까지 재시도하고 사라진 것을 확인한 뒤 다음으로 넘어간다.**
  덧붙여, 조건 간 비교는 시작값이 아니라 **관측 창 안의 증가분**으로 한다.
  시작값이 오염돼도 증가분은 살아남는다.

- **여러 조건을 한 스크립트로 이어 돌리면 조건 간 상태 누수를 먼저 의심한다.** 한 조건이
  환경을 부수는 동작(승격, 데이터 디렉터리 교체, 슬롯 삭제)을 하면 다음 조건은 그 잔해
  위에서 돈다. A23 은 물리 복원이 복구 인스턴스의 데이터 디렉터리를 갈아엎어 다음 논리
  복원이 0행을 돌려줬고, 그 0.06초가 "논리가 75배 빠르다" 로 표에 들어갔다. R01 은
  승격의 `RESET REPLICA ALL` 이 다음 조건의 복제를 끊었다. **조건마다 환경을 처음부터
  다시 세우거나, 파괴적인 조건을 별도 패스로 분리한다.**

- **부하와 실행이 끝난 뒤 행 수와 결과 수를 센다.** 위 두 사고 다 "빠른 값" 으로 남았다.
  실패는 느리게 오지 않는다. 아무 일도 안 하면 제일 빠르다. `if [ "$RN" -lt "$N" ]` 같은
  확인 한 줄이 그 회차를 표에서 빼 준다.

- **CDC·복제 실험은 필터가 무엇을 거르는지 매 조건 기록한다.** R04 에서 Debezium 하트비트가
  90초에 29번 돌았는데도 슬롯이 안 밀렸다. `publication.autocreate.mode=filtered` 가
  `table.include.list` 의 테이블만으로 publication 을 만들어 하트비트 테이블이 빠졌고,
  그 INSERT 를 pgoutput 이 걸러 냈다. 하트비트 테이블에는 행이 쌓이고 로그에도 경고가
  없어 **잘 도는 것처럼 보였다.** `pg_publication_tables` 를 조건마다 찍어 결과에 남긴다.

- **실패가 성공과 같은 모양으로 남는 자리를 먼저 의심한다.** 2026-07-31 하루에 같은 유형을
  열 번 밟았다. 공통점은 **에러가 안 나고 결과 파일이 정상 모양으로 남는다**는 것이다.

  | 자리 | 무엇이 조용히 어긋났나 |
  |---|---|
  | `numfmt --from=iec` (A08) | macOS 에 없는 GNU 명령. 명령 치환이 빈 문자열이 되어 SQL 이 `= ` 로 나가고, 실패가 워커 스레드 안이라 리사이즈 없이 잰 값이 리사이즈 결과로 저장됐다 |
  | `nproc`·`free` (여러 세션의 capture-env) | 같은 이유로 "호스트 CPU:  코어" 가 남는다. 성공한 줄과 모양이 같다 |
  | `SHOW subtransaction_buffers` (A19) | 17 에서 생긴 GUC. 16 에서는 에러 문자열이 그대로 값 변수에 들어간다 |
  | `-Dnetworkaddress.cache.ttl=0` (R03) | 그것은 `java.security` 가 읽는 **보안 속성**이라 `-D` 로 넘기면 무시된다. 시스템 속성은 `sun.net.inetaddr.ttl` 이다. "캐시 끔" 조건이 캐시 켠 채로 돌았다 |
  | `SELECT 'PING' FROM dual` 준비 확인 (A23) | 실패하면 sqlplus 가 질의문을 에코하므로 그 안의 `PING` 이 매칭된다. **준비 확인이 자기 질의문을 보고 통과했다.** `SELECT 'PI'\|\|'NG'` 로 바꿔야 결과에만 나온다 |
| 적재 없는 표 (A22·A09·R16) | 러너가 단계마다 볼륨까지 지우고 새로 띄운다. initdb 의 스키마는 들어가지만 적재는 안 된다. `COUNT(*)=0` 이 그대로 분모가 되어 `ZeroDivisionError` 로 끝나고, 그 직전까지의 출력은 정상으로 보인다 |
| 프로파일 뒤의 컨테이너 (R13) | `profiles: ["repl"]` 인 복제본은 기본 `up` 으로 안 뜬다. 스크립트는 `docker exec` 만 하고 "No such container" 를 찍은 뒤 진행해 두 조건 다 헤더만 남겼다 |
| 조건 사이의 `RESET REPLICA ALL` (R01) | 첫 조건이 복제본을 승격하며 복제 설정을 지운다. 다음 조건의 `START REPLICA` 가 붙을 곳을 몰라 복제본이 앞 조건의 표를 그대로 들고 있었다. 소스에 8행뿐인데 "남은 행 400" 이 찍혔다 |
| `nproc`·`free` + `set -e` (F03) | 없는 명령이 빈 값을 내는 데서 그치지 않고 실행을 끊는다. 기록용 한 줄 때문에 k6 가 아예 안 돌았고, 결과 표는 모든 조건 0.0ms 로 채워졌다 |
| 반동기 상태를 켠 직후 읽기 (R01) | `Rpl_semi_sync_source_status` 는 반동기 복제본이 붙어 ack 를 한 번 보내야 ON 이 된다. 설정 직후에 읽으면 OFF 라, 확인을 넣어도 조건이 안 선 채로 "유실 0건" 이 나온다 |

  세 가지를 규칙으로 둔다.
  1. **바꿨다고 믿는 값을 서버에 되물어 확인한다.** `SHOW`/`current_setting`/`pg_settings` 로
     읽어 기대값과 다르면 멈춘다. A18 의 `wal_compression` 확인이 그 모양이다.
  2. **준비 확인의 패턴이 질의문에 없어야 한다.** 찾는 문자열이 던진 문장 안에 있으면
     실패 출력이 그것을 그대로 돌려준다.
  3. **적재와 실행 뒤에 행 수나 결과를 세어 fail-closed 로 만든다.** A01 의 반복 측정에서
     `CREATE TABLE` 이 실패한 회차가 "0.16초에 끝난 아주 빠른 회차"로 표에 들어갈 뻔했다.

## 12. 재현 계층: 최소 재현과 실제 스택

버그가 실제로 사는 계층에서 재현합니다. 계층 선택은 세션마다 다릅니다.

- **순수 로직·언어 시맨틱 버그**는 최소 재현으로 충분합니다. 부동소수점 비교(F01, F17), 정수 오버플로, 컬렉션 오용 같은 문제는 프레임워크가 원인이 아니므로 짧은 자바 프로그램으로 메커니즘만 떼어냅니다. 웹 계층을 얹으면 곁가지만 늘고 핵심이 흐려집니다. 최소 재현 자체가 실력입니다.
- **프레임워크·인프라가 무대인 버그**는 그 무대로 재현합니다. 커넥션풀과 스레드 고갈(F03), 트랜잭션 전파, 역직렬화, WebSocket 백프레셔(F15)는 Spring Boot와 내장 서버, 실제 부하가 있어야 관측됩니다. 이때는 Gradle 멀티스테이지 Dockerfile로 빌드하고 k6나 부하 스크립트로 수치를 냅니다.
- 순수 로직 세션이라도 그 버그가 실제 스택의 어느 지점을 통과하는지 보이면 신뢰가 올라갑니다. F01은 최소 재현에 더해 Spring 주문접수 API로 "NaN이 실제 REST 검증을 통과하는가"를 실측했습니다(`spring/`). 도달성을 보이는 얇은 층이면 충분하고, 최소 재현을 대체하지 않습니다.
